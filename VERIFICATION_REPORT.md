# Verification report — every item the client reported

Companion to [`CLIENT_NOTES_CHECKLIST.md`](CLIENT_NOTES_CHECKLIST.md). One row per
checklist item, **204 rows** across groups A–N. (The checklist's own header says 203;
counting the rows gives A16 + B21 + C10 + D7 + E18 + F12 + G8 + H23 + I6 + J11 + K31 +
L20 + M10 + N11 = **204**. The extra row is a miscount in the checklist header, not a
missing item.)

Verified 2026-08-15 against: the code at `main`, the **live** backend
`backend-production-59d2.up.railway.app`, the **live** iOS app on simulator
`iPhone 17 Pro`, the production database (read-only), and the deployed dashboard.

---

## READ THIS FIRST — deployment state, and one thing you must still do

**All server-side work is live.** The backend and dashboard were both redeployed at
**2026-08-15 17:32:45 +03:00**, after the newest commit (`ffd4ed8`, 17:11:49). So every
fix in `main` is running in production, including the two security fixes.

Verified directly rather than assumed, twice:

- `POST /api/auth/login` for a password-holding account with no password now answers
  `{"code":"password_required", …}`. That `code` field was **added by `389fbe4` (A16)** —
  before it, the same response carried no `code` at all. **A16 is live.**
- An unauthenticated admin request answers `{"code":"auth_required"}`, a field **added by
  `5932a84` (A15)**. **A15 is live.**

The checklist's "Not deployed" notes on A1, A3, A15 and A16 are all **stale** — they were
written before this deploy.

> **Earlier in this pass the previous deployment (16:21) did *not* contain A16, and this
> report initially said so.** The redeploy landed while the report was being written. The
> finding was re-verified against the live API and corrected. Treat the probe results
> above as the authority, not the commit timestamps.

### The one deployment gap that remains: the mobile app

Everything above is server-side. **Flutter changes reach users only through an App Store
/ Play Store release**, and several fixes verified in this report exist only in the app
binary:

| Commit | What it fixes | Reaches users when? |
|---|---|---|
| `0e4bf5a` | **A3** — 3 of 4 support entry points opened the wrong screen | next app release |
| `389fbe4` (app half) | **A9** — deleted the silent session re-mint | next app release |
| `ffd4ed8` | **B21** — marketplace order card printed a token | next app release |
| `d9410f2` and the whole redesign batch | the four-state / design-system work | next app release |

So a user on today's installed build still sees the old support screens and the old
logout behaviour. **Ship a mobile release**, or roughly a third of the FIXED rows below
remain invisible to the client when he re-tests on his phone.

---

## Verdict counts

| Verdict | Count |
|---|---|
| **FIXED** | 79 |
| **ALREADY OK** | 43 |
| **STILL BROKEN** | 55 |
| **NEEDS OWNER** | 20 |
| **CANNOT VERIFY** | 7 |
| **Total** | **204** |

**122 of 204 (60%) are already correct** — fixed in this batch or never actually broken.
Roughly a third of the "still broken" rows are *partial* implementations, where the
feature exists but does not yet cover everything the client described; each says so.

> **Updated 2026-08-15, group-B sweep.** Nine rows moved from STILL BROKEN to FIXED:
> the whole English-leak group — B1, B3, B6, B9, B16, B20, B21 — and two rows outside
> it, E17 and M1. Four of the nine (B3, B6, B9 and half of B16) turned out to have been
> fixed already by an earlier commit whose report rows were never updated, so they were
> **verified, not re-fixed**. That is the point of re-checking rather than trusting the
> row: several confident claims in this report have been disproved the same way.

Plus **7 NEW FINDINGS** listed at the end — defects found during this pass that the
client had not reported. Three are security-relevant. **Four of the seven are now
fixed** (1, 2, 3 and 7).

### 1. Check these first — highest-severity STILL BROKEN

1. **Ship a mobile release.** Not a defect — but roughly a third of the FIXED rows below
   live only in the app binary, so the client will re-test on his phone and still see the
   old behaviour. See the deployment section above.
2. ~~**H13 — anyone with "edit users" can change the Super-Admin's phone number.**~~
   **FIXED 2026-08-15.** Confirmed first by reproducing it: `PATCH /api/admin/users/:id`
   never called the guard its eight sibling routes use, and an `admin`, an `employee`
   and a `supervisor` each rewrote a higher-ranked account's phone in a test driving
   the real request chain. One shared guard now covers every write on the resource.
   See the H13 row in section H.
3. ~~**H15 — 15 of 31 delete buttons bypass the trash.**~~ **FIXED 2026-08-15 for 13 of
   the 15.** The two left permanent are deliberate and named in the H15 row: the
   Notification Centre feed (an append-only log) and the blocked-words list (whose
   in-memory copy only its own code can refresh). Every other delete is now
   recoverable from المهملات.
4. ~~**B7 (new finding) — the user detail page sends the bcrypt `password_hash` to the
   browser.**~~ **FIXED 2026-08-15.** Confirmed at every staff tier, including
   `employee` — the generic detail route has no per-module permission, so the
   `sensitive_data` gate never applied to it. Ten production rows carry a hash, two of
   them `super_admin`. See NEW FINDING 1 below.
5. **E17 — profile edits say "saved" when they are not.** Only the name and photo go to
   review; the address saves instantly, and photo *removal* skips review entirely. The
   app shows "saved" either way, so the user is misled.
6. **B1 — the app's notification type filter still prints raw English enums**
   (`marketplace_order_approved`, `system_test`, …) in the Arabic UI. Reproduced on the
   simulator today; `notifications_screen.dart:592` renders `Text(type)` with no
   translation.
7. **E1 / E7 — phone numbers on the dashboard.** The app side is fixed; the dashboard
   prints phones with no LTR isolation, so digit groups reorder in Arabic. Separately,
   the dashboard **edit** form does not trim spaces, and a phone stored with a space can
   no longer sign in.
8. **M1 — project and achievement transaction codes are never issued.** The namespaces
   are seeded but unused, so a project donation files under `GEN-`, defeating the
   per-section tracking the client asked for.

### 2. Now fixed — worth a spot-check

- **A1** — the `Database error.` banner on المساهمات is gone and live. All 14
  contributions load.
- **A15** — an ordinary app user can no longer reach the dashboard; live.
- **A7 / A8 / A9** — Back no longer offers to log you out, no black screen, and logout
  now really ends the session (verified end-to-end on the simulator today).
- **A14** — المهام → عرض opens a real detail page and has a "Back to list" button.
- **B5, B11, B12, B13, B14, B15, B17, B18** — every named dashboard dropdown now shows
  Arabic words instead of `approved` / `needs_changes` / `matched` / `published`.
- **D3, D4, D5, D6** — the موظف option exists in both dropdowns, "أي يوم" is now
  "الأيام", and 22 of 22 action columns carry the إجراءات header.
- **E9, E10, E11, E13, E14, E16** — password editing, registrations detail + export,
  إيقاف/أرشفة on delivery, the عرض button in دليل المدينة, the no-longer-jumping
  dropdown, and international phone numbers at sign-in.
- **G3, G4, G5, G6** — all four misaligned tables. Headers and cells now come from one
  array, so a column shift is structurally impossible.
- **L1, L3–L9, L11–L18** — the client's premise that "the specified fields were never
  put in" is **wrong** for 16 of 20 rows; the beneficiary form alone carries ~90 spec
  fields with every conditional reveal wired.
- **F1, F2, F5, F6, F8, F9, F11, F12** and **H2–H7, H9, H12, H16–H19, H21, H22** — most
  of the requested permissions/audit/export subsystem already exists.

### 3. Needs the owner's decision (not defects)

**A1 (second half)**, C4, C5, C8, D1 wording, E8 (which states show a reason), **E17
scope (new — see 4 below)**, F10, I1, I3, J11, K31, N1–N11. The four that block other
work:

1. **Terminology (I1/I3/N7).** Is the giving section **المساهمات** or **ادعمنا**? Is
   الكفالات becoming **المعونات**? The English already says "Assistance"; the Arabic is
   waiting on you. Nothing else in group I should move until you answer.
2. **In-kind contributions "sent to a user" (A1).** The schema has no recipient concept —
   `in_kind_donations` records only who *gave*. Delivering what you expect needs a new
   recipient field, a notification, and a screen in the app. It was flagged, not invented.
3. **`OTP_DEMO_ENABLED` is on in production with `OTPIQ_API_KEY` unset.** The demo code
   is returned in the response body, so it proves nothing. Set the SMS keys, confirm real
   delivery, *then* turn demo off — in that order, or every user is locked out.

   **Updated 2026-08-15, and this is now the one blocker on finishing sign-in.** Under
   the settled design (A16 — "OTP for account creation only, password to sign in") a code
   is only ever spent to choose a **first** password, and each of the 36 passwordless
   accounts can be claimed once, so the demo code's reach is bounded. **What it still
   blocks is "forgot my password".** That flow is the same flow with the bound removed —
   with a public code it would let anyone take over any account, permanently — so it has
   deliberately **not** been built. It becomes safe the day `OTPIQ_API_KEY` is set and a
   real code arrives out of band. Until then, resets are yours to do from
   المستخدمون → تعديل. This is a finding, not a preference: there is no version of
   self-service reset that is safe while the code is printed to whoever asks for it.

4. **Which profile fields go through review (E17).** Your note says profile edits must
   not save until an employee approves. Today exactly two do: the **name** and the
   **photo**. **Address** saves the moment the user presses save, and **removing** a
   photo takes effect immediately — a user can therefore delete an approved photo
   without anyone reviewing it, which may or may not be what you intend.

   The app no longer misreports any of this: it now says exactly which parts are waiting
   and which are already saved. Widening the rule is a separate, deliberate change —
   a migration to make `address` reviewable, a queue entry for a removal, and the
   dashboard screens to review both — and it is a policy question rather than a bug:
   **should an address change, and should deleting a photo, wait for an employee?**
   Answer that and it is a day's work; guessing at it is not.

### 4. What could not be verified — 7 of 204

**The dashboard login session had expired** when this pass began (`localStorage` is
empty; the page redirects to `/login`). Entering credentials is not something this pass
will do, so four dashboard-behaviour rows could not be watched on screen and are
recorded as CANNOT VERIFY: **A2** (force logout), **A10** (no notification for a
submitted request), **A13** (mark-completed button vanishing) and **E12** (partner logos
— the rendering code is correct, so this is about what the stored paths hold).

The other three: **A4** — could not reproduce; the carousel's last card *did* render, and
the final two entries are junk rows genuinely named `ب` and `س`. **A5** — no account
named نور exists in production; the number quoted belongs to user #17, named "M".
**A6** — depends on A5's account and is role-gated anyway.

Several further dashboard rows (**A11, A12, G1, G2, G8** and most of group H) were
settled from **code alone** because the layout or gate is unambiguous there. They carry
a verdict, and each says "code-verified only" where a live look would have added
confidence. Role-gated **beneficiary and volunteer** app screens were also unreachable —
the simulator is signed in as a donor — and rows depending on them say so.

> **Process note:** OPOS MCP is listed but unauthorized in this non-interactive session,
> so no tasks were created or tracked there. This was a **read-only verification — no
> code was changed.**

---

## A. SEVERE — blocks a working flow

| ID | What the client reported | Verdict | Evidence | How YOU can check it |
|---|---|---|---|---|
| A1 | `Database error.` on المساهمات; in-kind sent to a user never arrives | **FIXED** | `5db972f`, deployed 16:21. One guest with `phone IS NULL` broke the scan for the whole list; now `COALESCE(u.phone,'')`. Second half (in-kind → a user) is a **missing feature**, not a bug: `in_kind_donations` has only `donor_user_id`, no recipient column — see "Needs your decision" #2 | Dashboard → المساعدات والحملات → المساهمات. The red banner should be gone and all 14 rows listed. |
| A2 | Force logout (تسجيل خروج قسري) does not work | **CANNOT VERIFY** | Dashboard session expired; I will not enter credentials. Code is complete: route `main.go:830` → `admin_status.go:257-276` revokes every token row, and `auth/token.go:177-179` refuses a revoked token on the next request | Dashboard → المستخدمون → الإجراءات → إنهاء الجلسة on a test account, then have that person tap anything in the app — they should be thrown back to sign-in. |
| A3 | Contact-support chat does not work | **FIXED — server live, app awaits release** | `3ba3fce` (dashboard Reply button + notification; **nothing in the dashboard had ever called the reply endpoint**, so a reply was impossible for anyone using the product) — live since 17:32. `0e4bf5a` (3 of 4 app entry points opened a bare form, not the real screen) is in the app binary and needs a store release | Dashboard → الدعم → Reply on a ticket works today. On the phone, the right screen appears only after the next app release — until then 3 of the 4 doors still open the old bare form. |
| A4 | City Guide: the last slide cannot be displayed | **CANNOT VERIFY** | Could not reproduce. I scrolled the place carousel to its final card on the simulator and it **rendered**. The last two entries are genuinely named `ب` and `س` with category `ب`/`ر` — junk test rows, confirmed in the live API (`GET /api/community`, 9 items) | Open دليل المدينة and swipe the cards under the map to the very end. If a card is blank rather than showing a single Arabic letter, tell us — otherwise this was the test data, not a fault. |
| A5 | Dashboard shows `07701111111` for نور كاظم | **CANNOT VERIFY** | Read-only query of production: **no account named نور exists**. The number (stored canonically `9647701111111`) belongs to **user id 17**, whose profile name is `M`. Also: **0 of 46 phone numbers contain whitespace**, so nothing is malformed in storage | Tell us the account you meant — search المستخدمون for it. The number you quoted is on user #17, named "M". |
| A6 | Some app screens stop working under that account | **CANNOT VERIFY** | Depends on A5's account, which does not exist; and the simulator is signed in as a donor, so another user's session cannot be reproduced without their credentials | Once you identify the account in A5, tell us which screens broke and we will reproduce it. |
| A7 | Back from the main screen pops a logout message | **FIXED** | `2a87ebc`. `dashboard_screen.dart:160-179`: from any non-Home tab Back returns to Home; from Home it asks **"الخروج من التطبيق؟"** — an exit question, not a logout offer | On Android press the back button on the home screen: it should ask about closing the app, never about signing out. (iOS has no hardware back, so this is Android-only.) |
| A8 | Confirming that logout turns the screen black and freezes | **FIXED** | `2a87ebc`. The cause is documented in the code at `dashboard_screen.dart:161-162` — popping the root route left an empty navigator. It now calls `SystemNavigator.pop()` instead, which backgrounds the app on Android | On Android, back → confirm: the app should go to the home screen like pressing Home, never freeze on black. |
| A9 | Logout does not log out — session survives an app kill | **FIXED** | **Verified live today.** Signed out, force-quit the app, cold-launched → landed on the welcome screen, not the dashboard. `389fbe4` removed `ensureApiSession()`, the silent re-mint that POSTed the remembered phone number. See NEW FINDING 6 for a narrow race that remains | Sign out, swipe the app away completely, reopen it. You should see "توازن واستقرار من أجل حياة أفضل!" and a sign-in button, not your dashboard. |
| A10 | A submitted help request produced no dashboard notification | **CANNOT VERIFY** | Dashboard session expired. Submitting a real request would also write production data, which this pass does not do | Send a test help request from the app, then watch the bell on the dashboard for ~10 seconds. |
| A11 | Selecting `needs_changes` crashes the page layout | **FIXED (code-verified only)** | `06ab6c0`. `StatusCell.tsx:97-113` now sizes the dropdown from the **widest** option in the list, not the selected one, and pins it in pixels — the comment states the intent: the box never resizes when the selection changes | Dashboard → المستفيدون, change any row's الحالة to "يحتاج تعديلات". The columns beside it must not jump. |
| A12 | Volunteer action buttons vanish after final approval | **FIXED (code-verified only)** | `VolunteersPage.tsx:918-923` always renders all 8 statuses, and the server validates against a flat allowlist rather than a one-way flow (`admin_status.go:415-420`), so a request can always be reopened or reversed. Caveat: the separate per-status action menu still shows "—" once completed (`VolunteersPage.tsx:947-952`) | Dashboard → المتطوعين → تسجيلات المهام, approve a row, then change its status back — the dropdown should still offer every option. |
| A13 | The `mark completed` button disappears when clicked | **CANNOT VERIFY** | Dashboard session expired; this is a click-time behaviour that cannot be settled from code. The button itself exists and is translated at `VolunteersPage.tsx:859`, `MissionsPage.tsx:258`, `VolunteerBoardPage.tsx:112` | Dashboard → المهام, press "تحديد كمكتمل" and watch: if the button vanishes with nothing replacing it, screenshot it for us. |
| A14 | المهام → عرض shows "مورد غير معروف"; no way back | **FIXED** | `af3e8c9`. `volunteer_missions` is a registered detail resource at `backend/internal/handlers/admin_detail.go:69`, and the detail page carries a back control at `admin-web/src/pages/DetailPage.tsx:176` (`common.back_to_list`) | Dashboard → المهام → الإجراءات → عرض. You should see the mission's fields and a "رجوع للقائمة" button. |
| A15 | Ordinary app users can reach the dashboard | **FIXED — live** | `5932a84`, deployed. `staff_tier` is now the only authority; the legacy `is_admin` flag grants nothing. Confirmed against production: the API's refusal now carries `{"code":"dashboard_access_required"}`/`auth_required`, fields this commit introduced | Ask a normal app user to open the dashboard URL and sign in — they should be refused. |
| A16 | A phone number alone bought a session, incl. a Super-Admin's | **FIXED — live; final design written, not yet deployed** | `389fbe4` (deployed 17:32) closed the original hole and **is live** — probed against production: a password-holding account with no password submitted answers `{"code":"password_required"}`, and a phone with no password gets `401 otp_required`. Since then the owner settled the design — *"OTP for account creation only, password will be used for sign in to the app later"* — and it is implemented but **not deployed**: `/auth/otp/verify` no longer issues a session at all, only a single-use ticket to choose a FIRST password, so a code can never open an account that already has one (that was still possible until today, for the ten accounts that do). Each of the **36 passwordless accounts (of 46)** can be claimed exactly once. **⚠️ Two things still need you** — see "Needs your decision" #3 and the A16 notes in the checklist | Nothing to click yet, and **do not** hand out passwords manually any more: **(1)** set `OTP_STAFF_DEMO_CODE` to a random six digits and give it privately to the holders of **id 1** and **id 34** so they can claim their accounts and choose their own passwords, then delete the variable; **(2)** set `OTPIQ_API_KEY` when the account is funded — until it is set there can be **no self-service "forgot password"**, because with the demo code public it would let anyone take over any account. Until then, a forgotten password is reset by you from المستخدمون → تعديل. |

## B. English leaking into the Arabic UI

| ID | What the client reported | Verdict | Evidence | How YOU can check it |
|---|---|---|---|---|
| B1 | Notification type filter shows raw English enums | **FIXED (both halves)** | **App:** `d498453` routed the dropdown through `localizedTag` and added the whole 81-value `notification_type` vocabulary in en + ar. **Dashboard:** `98629a7` had routed the النوع column through `useStatusLabel` but added no words for it to find, so the column still printed `marketplace_order_approved`; the same 81 values, same wording, now sit in `status.*` in `en.ts`/`ar.ts`. Verified by parity check rather than by eye: every concrete `Type:` literal in `backend/internal/notify/templates.go` (68 of them, plus the 3 interpolated families expanded to their members) resolves in both files — 0 missing, en/ar key sets identical at 209 each. ckb/kmr fall back to English by design and are listed in `TRANSLATION_REQUEST.md` | App in العربية → الإشعارات → التصفية حسب النوع, and Dashboard → الإشعارات → the النوع column. Both should read Arabic phrases, not machine text. |
| B2 | "Support Assistant / Ask me anything" in English | **ALREADY OK** | `e461fc2`. **Verified live**: the card reads "مساعد الدعم / اسألني عن أي شيء — سأرشدك خلال التطبيق" | App in العربية → الرسائل, read the top card — both lines Arabic. |
| B3 | Place detail shows a raw JSON array of sectors | **FIXED** | `cb76db0`. `DetailPage.tsx:97-103` now branches on `Array.isArray` BEFORE the object branch, sends each element through `statusLabel` — the same vocabulary the list pages use — and joins with the Arabic comma. Generic rather than per-column, because several resources carry a `TEXT[]` (city sectors, volunteer `skill_tags`, marketplace labels, `field_privacy`) | Dashboard in العربية → دليل المدينة → عرض. The "sectors" row still shows `["commercial","government"]`. |
| B4 | Login screen is in English | **ALREADY OK** | **Verified live**: "مرحبا بعودتك / أدخل رقم هاتفك وسنرسل لك رمز تحقق. / متابعة / المتابعة باستخدام Google / الدخول كزائر" | Sign out and read the sign-in screen in العربية — no English except the Google brand name. |
| B5 | Place list shows status `approved` in English | **FIXED** | `98629a7`. `CityGuidePage.tsx:277` now `statusLabel(e.status)`; `ar.ts:1184` `approved: 'مقبول'` | Dashboard in العربية → دليل المدينة, الحالة column reads مقبول. |
| B6 | Untranslated English remains; wants one global fix | **FIXED** | `cb76db0`. `FieldSpec` gained `placeholderKey`, resolved at `EditModal.tsx:247` and applied at both render sites — the same shape as `labelKey`, not a second mechanism. All 11 English hints are converted. The 11 `placeholder` literals that remain are format tokens with no English in them (`IQD`, `0`, `YYYY-MM-DD`, a JSON sample, a URL sample), deliberately left bare and documented at `EditModal.tsx:44-47` | Dashboard in العربية, open any تعديل form and read the **grey hint text inside empty boxes** — that is the last English left. |
| B7 | User detail page renders raw DB column names | **ALREADY OK** | `af3e8c9`. `DetailPage.tsx:185` `fieldLabel(k)`; `ar.ts:1363-1370` carries all 12 named columns; header is `t('common.read_only_view')` → "عرض للقراءة فقط." **Separate security issue found here — see NEW FINDING 1** | Dashboard in العربية → المستخدمون → عرض. Every label on the right should be an Arabic word. |
| B8 | Numeric ids where names belong | **ALREADY OK** | `af3e8c9`. `DetailPage.tsx:196-199`: `role_id` → role word; any `_by` column → `"<full name> (#8)"` resolved from the users list | Dashboard → المستخدمون → عرض: "روجع بواسطة" shows a person's name, "الدور" a role word. |
| B9 | Raw ISO timestamps instead of a localized date | **FIXED** | `cb76db0`. `DetailPage.tsx:113-114` routes ISO values through the existing `formatDateTime`/`formatDateOnly`. Matched on the VALUE, not the column name — `created_at` is a reliable signal but `next_due_date`, `dob` and `event_date` follow no one convention, while the two ISO shapes are unmistakable and free text cannot collide with them (`:55-56`) | Dashboard → المستخدمون → عرض: "تاريخ الإنشاء" still reads `2026-06-14T11:13:32.50385Z`. |
| B10 | Edit-campaign modal is 100% English | **ALREADY OK (2 hints remain)** | Title `CampaignsPage.tsx:339` `t('common.modal_edit')`; all 13 fields carry `labelKey`; buttons `t('common.cancel')` / `t('common.save_changes')`. Residual: 2 placeholders (part of B6's 11) | Dashboard in العربية → الحملات → تعديل: every label and both buttons Arabic; only the faint hint inside المستفيدون is English. |
| B11 | `needs_changes` shown raw | **FIXED** | `8f404e8`. `BeneficiaryPage.tsx:519-520` `statusLabel(s)`; `ar.ts` `needs_changes: 'يحتاج تعديلات'` | Dashboard in العربية → المستفيدون → الحالة dropdown: all Arabic phrases. |
| B12 | `matched` shown raw | **FIXED** | `af3e8c9`. `MarriagePage.tsx:235` `statusLabel(s)`; `ar.ts` `matched: 'تم التوفيق'` — one of the two wordings you suggested | Dashboard in العربية → زواج → الحالة dropdown shows "تم التوفيق". |
| B13 | `published` shown raw; rename "متخفي" | **FIXED** | `2a87ebc`. `MediaPage.tsx:315` `statusLabel(s)`; `published: 'منشور'`, `hidden: 'مخفي'`. **`متخفي` = 0 occurrences repo-wide** | Dashboard in العربية → الاخبار والاعلام → الحالة: منشور / مسودة / مخفي, and "متخفي" nowhere. |
| B14 | "Add place" button in English | **FIXED** | `af3e8c9`. `CityGuidePage.tsx:342` `t('common.city_add_place')` → `ar.ts:606` "+ إضافة مكان" | Dashboard in العربية → دليل المدينة: the top button reads "+ إضافة مكان". |
| B15 | "mark completed" in English | **FIXED** | All three call sites use the key (`VolunteersPage.tsx:859`, `MissionsPage.tsx:258`, `VolunteerBoardPage.tsx:112`) → `ar.ts:966` "تحديد كمكتمل" | Dashboard in العربية → المهام: the button reads "تحديد كمكتمل". |
| B16 | Per-section Arabization sweep, 12 named sections | **FIXED (12 of 12)** | 10 sections carried zero hardcoded English already. The 2 that leaked were **المستخدمون → عرض** and **دليل المدينة → عرض** — both `DetailPage.tsx`, and both closed with B3 and B9 above. The sidebar collision found alongside them is closed too: `cb76db0` renamed `missions` to "مهام التطوع", so the two entries the sidebar shows are no longer both "المهام" (ckb/kmr still collide — a translator's row in `TRANSLATION_REQUEST.md`, not a defect, since both fall back to English). One thing this row got wrong on the first pass and is worth recording: الاشعارات was passed as clean because the page has no hardcoded English — but its النوع column was printing raw enums as **data**. That is B1, now fixed | Walk the sidebar in العربية top to bottom. Both "عرض" pages now read as Arabic labels with Arabic values, and the two mission/task entries are named apart. |
| B17 | الأولوية column values in English | **ALREADY OK** | `af3e8c9`. `BeneficiaryPage.tsx:419` `statusLabel(r.priority_level)`; `ar.ts` low/medium/high/urgent → منخفضة/متوسطة/عالية/عاجلة | Dashboard in العربية → المستفيدون: the الأولوية column and its dropdown are Arabic. |
| B18 | Gender dropdown not Arabized | **ALREADY OK** | `af3e8c9`. `UsersPage.tsx:25` values route through `statusLabel` → ذكر / أنثى / آخر | Dashboard in العربية → المستخدمون → تعديل → الجنس. |
| B19 | "حالة دورة الحياة" is wrong wording | **NEEDS OWNER** | The complained-of string is **gone — 0 occurrences repo-wide**. `2a87ebc` changed it to `ar.ts:1301` "حالة الحملة". Whether that reads correctly is your call, not a defect | Dashboard in العربية → الحملات → حملة جديدة: the first field now says "حالة الحملة" — confirm that wording. |
| B20 | Volunteer-requests dropdown headers unnamed/English | **FIXED** | Options were already translated. The naming half is now done: each filter select is wrapped in a `<label className="filter-field">` carrying a visible caption — الحالة / المهارات / التوفّر — which also gives the status select the accessible name it never had. Captions reuse `col.status` / `col.skills` / `col.availability`, already present in **all four** locales, so no new vocabulary and no invented Kurdish. The same fix was applied to the تسجيلات المهام tab's status filter, which had the identical defect. Layout measured in the browser against the real built CSS: all seven controls in the row sit at top 71 / bottom 107, captions 2px above | Dashboard in العربية → المتطوعين → الطلبات: a small grey word now sits above each of the three boxes saying what it filters, and the boxes still line up with the search box and buttons. Same on the تسجيلات المهام tab. |
| B21 | Choosing a language must translate ALL app text | **FIXED for Arabic — Kurdish still needs a translator** | `d498453` added the 19 missing literals (الكفالات, مساهماتي, empty chat, Services list, volunteer mission cards). The "floor, not a ceiling" caveat is now discharged rather than restated: `test/localization/arabic_map_purity_test.dart` asserts the property over the **whole map** instead of a list someone remembered to write — every `_en` key has an `_ar` entry, no `_ar` value equals its English, and no Latin letter survives into an Arabic value once `@placeholder` slots and 9 justified brand tokens (Google, Firebase, FIB, IQD, GPS, VIP…) are removed. All three pass at 2112/2112 keys. The test was proved to have teeth before being trusted: re-injecting the two original B21 shapes — an `_ar` entry pasted from English, and an `_en` key with no `_ar` at all — fails it. Kurdish is deliberately untouched and falls back to English; 236 keys are itemised in `TRANSLATION_REQUEST.md` | App in العربية: الكفالات, مساهماتي, an empty chat and the support form now read as Arabic. Switch to Kurdish and English still appears — that is the fallback working, waiting on a translator, not a fault. |

## C. Duplication / information architecture

| ID | What the client reported | Verdict | Evidence | How YOU can check it |
|---|---|---|---|---|
| C1 | "شركاؤنا" appears 3–4 times | **STILL BROKEN (reduced)** | **4 user-visible doors** remain to the same screen: `dashboard.dart:2109` (Home "See all"), `dashboard.dart:2172` (each partner logo), `settings_section.dart:97` (Settings), `proposal_services_section.dart:189` (Services tile). `744a357` and `d421190` removed others. **Confirmed live**: شركاؤنا is in the الإعدادات tab | App in العربية as a donor: Home → شركاؤنا, then الإعدادات → شركاؤنا, then الخدمات → الشركاء. Three routes, one screen. |
| C2 | Lists on the City Guide that do not appear | **STILL BROKEN** | Two silent disappearances. (1) The sector chip row is behind `if (sectors.isNotEmpty)` (`community_services_section.dart:339`) and its loader swallows every error — `catch (_) { sectors.clear(); }` (`community_controller.dart:77-87`): no loading, error or retry, the row just vanishes. (2) `dashboard.dart:368` prints the heading "Browse by category" unconditionally while the capsules return `SizedBox.shrink()` when empty. **Also found live: all 9 city-guide entries have `sectors: []`**, so the filter chips match nothing | App → دليل المدينة, turn on airplane mode and reload: the chips vanish with no message while the map correctly shows an error with retry. |
| C3 | Notification icon duplicated top AND bottom | **FIXED** | `f2119e1` removed the Alerts tab. **Verified live**: exactly one bell, top bar only; the bottom bar is الرئيسية / المتجر / الفعاليات / دليل المدينة / الإعدادات | App, any screen: one bell top-right, none in the bottom bar. |
| C4 | من نحن / اتصل بنا repeat with different details | **NEEDS OWNER** | Deliberate and spec-mandated per `e10d8ae`: the marriage service and city guide "must contain details, phone numbers and social media links different from those used for humanitarian assistance". 6 independently editable pages exist | Dashboard: open من نحن, then الزواج → من نحن, then دليل المدينة → من نحن. Decide whether you still want three different sets of contact details. |
| C5 | "Send a request" duplicated three times | **NEEDS OWNER** | Only **1** is in live UI: the beneficiary Home hero button (`dashboard.dart:467`). The second is on an **orphaned screen** reachable only through the chatbot (`sponsorship_section.dart:80`), the third is the chatbot route itself. The quick-actions row has **zero** submit entries — they were deliberately deleted (`dashboard.dart:532-537`) | Sign in as a مستفيد in العربية: Home has exactly one إرسال الطلب and the quick-actions row has none. Tell us if the third one you saw was via the assistant chat. |
| C6 | Move الخدمات off home into الملف الشخصي | **FIXED** | `f2119e1` + `e634261`. `profile_menu_screen.dart:122-127`; `ProposalServicesSection` appears **0** times in `dashboard.dart`. **Confirmed live** in the profile menu | App: tap your photo top-right → الخدمات. Scroll all of Home — no الخدمات there. |
| C7 | Move خدمات المجتمع into الملف الشخصي | **FIXED** | `e634261`. `profile_menu_screen.dart:139-144`; 0 occurrences in `dashboard.dart`. **Confirmed live** | App: photo top-right → خدمات المجتمع. Not on Home. |
| C8 | Beneficiaries: move الرسائل out of the bottom bar | **NEEDS OWNER** | The premise no longer holds — **الرسائل is not in the bottom bar for any role** (`dashboard_screen.dart:54-85` is a `static const` list with no role branching). `f2119e1` moved it to the **top bar**, beside the avatar, not inside the profile menu | Sign in as a مستفيد: the bottom bar has no الرسائل; the icon is top-right next to the bell. Tell us if that placement is acceptable. |
| C9 | دليل المجتمع and تبرعات عينية need the same repeated fixes | **STILL BROKEN (half done)** | The D6 half is done — both declare `header: t('common.actions')` (`CommunityPage.tsx:134`, `InKindPage.tsx:185`). The G1 half is not: both عرض links go to the same unfixed `DetailPage` | Dashboard → دليل المجتمع and → تبرعات عينية in العربية: the last column is named, but عرض still shows the wide label/value gap. |
| C10 | Split دليل المدينة and الزواج into standalone tabs | **ALREADY OK** | `f2119e1`. Both are top-level bottom-nav tabs (`dashboard_screen.dart:67-78`). **Caveat: the marriage tab is labelled "الفعاليات" (Events), not خطوبتي** — confirmed live — which weakens the visual distinction you asked for | App: the bottom bar has five tabs including both sections. Note the marriage tab reads "الفعاليات" — probably not the wording you want. |

## D. Dropdowns / labels

| ID | What the client reported | Verdict | Evidence | How YOU can check it |
|---|---|---|---|---|
| D1 | User-type dropdown contains "بلا" and "—" | **STILL BROKEN** | Both reproduce from code. `api-types.ts:737-738` returns the literal `'—'` for role 0; because `'—'` is not in the allowed list, `StatusCell.tsx:117` injects an **extra** option for it — so `—` and `بلا` appear side by side. Neither rename was done: `ar.ts:1213` still `none: 'بلا'` (not زائر) and `ar.ts:1164` `marriage: 'زواج'` (not خاطب) | Dashboard in العربية → المستخدمون → نوع المستخدم dropdown: you will see both `—` and `بلا`, and "زواج" where you asked for "خاطب". |
| D2 | Marriage applicant details missing entirely | **FIXED** | `e634261`. Migrations 079/080/082 add the whole field set to `marriage_profiles`; `admin_detail.go:88` does `SELECT *` so **every** column renders, and `DetailPage.tsx:198-199` resolves the user to a real name. The **list** page shows only id/code/gender/age/city — which is likely why it read as missing | Dashboard → الزواج → الإجراءات → **عرض** (not the list). All identity, housing, education and health fields are there. |
| D3 | مشرف dropdown needs a "موظف" option | **FIXED** | `2a87ebc`. `UsersPage.tsx:356` includes `'employee'` → `ar.ts:1169` "موظف". The tier is real in the backend (`permissions.go:34`, migration 015) | Dashboard in العربية → المستخدمون → صلاحية الوصول dropdown: موظف is there. |
| D4 | الدور dropdown needs "موظف" | **FIXED** | `af3e8c9` / `ad5e6e8`. `UsersPage.tsx:24` includes `'employee'`, round-tripped as role 4 both ways (`UsersPage.tsx:102`, `api-types.ts:732-733`) | Dashboard in العربية → المستخدمون → نوع المستخدم dropdown: موظف is an option. |
| D5 | Rename "أي يوم" | **FIXED** | `af3e8c9`. The diff is literally `- 'أي يوم'` / `+ 'الأيام'`; `ar.ts:1079`, used at `VolunteersPage.tsx:515` | Dashboard in العربية → المتطوعين: the day filter now reads "الأيام". |
| D6 | Every table's last column has an empty header | **FIXED** | `0287b0c`. **22 of 22** action columns carry `header: t('common.actions')`. The 5 remaining blank headers are leading thumbnail/checkbox columns, plus one +/− expander in سجلات التدقيق. Minor: `ar.ts:634` says "إجراءات" (indefinite) where you wrote "الإجراءات" | Dashboard in العربية, check التبرعات، المستفيدون، السوق: the rightmost column now says "إجراءات". |
| D7 | Replace the long language list with a dropdown | **FIXED** | `e634261`. **Verified live**: the profile menu shows a single "اللغة" row with the current code and a chevron, opening a 4-language picker sheet (`settings_section.dart:441-501`). A legacy always-expanded list survives at `profile.dart:896` but is reachable **only** through the chatbot | App in العربية: photo top-right → اللغة is one row with an arrow. |

## E. Forms / validation / display

| ID | What the client reported | Verdict | Evidence | How YOU can check it |
|---|---|---|---|---|
| E1 | Phone number still displays incorrectly | **STILL BROKEN (dashboard only)** | App side **fixed and verified live** — the profile card shows `+9647503091050` correctly, using LTR isolation (`verification.dart:132`, `edit_profile.dart:562-564`, `login.dart:280-281`). Dashboard side not: `lib/phone.ts:16` returns space-separated groups and all render sites print it bare (`UsersPage.tsx:303`, `RegistrationsPage.tsx:147`, `AppShell.tsx:439`); the spaces are bidi-neutral so the groups reorder under RTL. The team knew the fix — `formatId.ts:16` does exactly this for ids, with a comment | Dashboard in العربية → المستخدمون: compare a phone in the table against the real number. If it reads `2031 858 0750` instead of `0750 858 2031`, it is still wrong. |
| E2 | Phone should be editable; identifier should be a code | **FIXED** | `580ed0b` + `d0b19be`. Phone is an editable field (`UsersPage.tsx:39`) written at `admin_edit.go:1468-1475`; the id column renders `fmtId()` → `#T12` | Dashboard → المستخدمون → تعديل: the phone box is typeable, and the first column reads `#T12`, not `12`. |
| E3 | Constrain image size, except medical/house/ID documents | **FIXED** | `58ffa6f`. Both halves were missing, and inverted: no `maxWidth`/`maxHeight` was passed anywhere, so nothing was ever resized (an 8000px shot uploaded at 8000px), while every attachment took the avatar path and was JPEG-compressed **twice** — picker at 85, then cropper at its default 90. The lossy step was applied and the cheap one skipped. `PhotoFidelity` now names the two cases; `document` sets every knob to null, which is what makes image_picker return the original file rather than a re-encoded copy (100 still re-encodes). Covers all 8 beneficiary + 8 volunteer + 3 marriage attachments; personal/profile photos stay compressed, bounded at 1600px. **One judgement to confirm:** a document now skips the crop step entirely, because a crop always re-encodes and "original size" and "cropped" cannot both be true — so a user can no longer trim a document in-app. If you would rather keep trimming and accept one re-encode, give `document` a `cropQuality` of 100 in `shared/utils/image_pick.dart`; it is a one-line change | App: register as مستحق and attach a تقرير طبي, then open it from the dashboard and zoom on the small print — it should now be as sharp as the original on the phone. **Needs an app release to reach the phone.** |
| E4 | Login error text red on an unsuitable background | **FIXED** | `fa861eb`. Reproduced first: `verification.dart` and `registration_form.dart` both hardcoded `Colors.redAccent` on the GlassPanel card — measured **3.19:1**, under the 4.5:1 AA floor, and wrong in dark mode where the card moves and the red does not. A hardcoded colour is invisible to a palette guard by definition, which is why `contrast_test.dart` never saw it; all three auth screens now share one `AuthInlineError` widget drawing the themed `consequence` token, and `test/design/auth_error_contrast_test.dart` reads the colour back off the rendered OTP screen in both themes (red before the fix). Left alone deliberately: the nav unread badge and the 'urgent' sponsorship tag also use `redAccent`, but neither is error text | App sign-in → متابعة → type a wrong 6-digit code. The red line should now match the weight of the error on the sign-in screen behind it, in both light and dark. **Needs an app release to reach the phone.** |
| E5 | No countdown before the OTP can be re-requested | **FIXED** | `fa861eb`. The finding that mattered: this was never a missing limit. The server has enforced a 60-second per-phone cooldown all along (`auth.OTPResendCooldown`, applied at `backend/internal/handlers/auth.go:611`) and answers a refusal with `retry_after` — the app read past it and printed a bare "Please wait", so the button looked broken rather than early. `LoginController` now owns the countdown, seeded from a successful send and re-seeded from `retry_after`, always taking the **longer** of the two because the progressive phone lockout answers in hours. The control is disabled while it runs and says `m:ss`. Also fixed with it: the button and its action could disagree — mid-send it still read "Resend OTP" while its handler navigated away to sign-in, discarding the code. **No backend change was needed or made** | App: on the code screen tap "Resend" three or four times quickly — you should see "Resend in 0:45" but the button just stays tappable. |
| E6 | City Guide: how to add a link is unclear | **STILL BROKEN** | `CityGuidePage.tsx:51` defines the link field as a bare text input with no placeholder, no example and no validation — while the two neighbouring link-ish fields **did** get examples (`:61-62` social links, `:66-67` hours) | Dashboard → دليل المدينة → إضافة: the "Link / Website" box is empty with no example, while "Social links" below it shows a greyed-out one. |
| E7 | Trim spaces in phone fields programmatically | **STILL BROKEN (dashboard only)** | App side complete (`login.dart:174`, `guest_upgrade.dart:60`, `registration_form.dart:1067-1068`). Dashboard **edit** form does not trim — `EditModal.tsx:170` posts the value as typed; only the create path trims (`UsersPage.tsx:517`). The server does `TrimSpace` (`admin_edit.go:1469`) which removes edge spaces but **keeps inner ones**, and never calls `NormalizePhone`. Good news from production: **0 of 46 stored phones contain whitespace today** | Dashboard → المستخدمون, edit a test user's phone with spaces and save, then try to sign in with that number in the app. If it says the account does not exist, the spaces were stored. |
| E8 | Rejection reason never appears in the dashboard | **FIXED** | `cd6efe9` (+ `af54575` today for beneficiary cases). `ProfileChangesPage.tsx:104-110` renders the reviewer name and the reason; the backend now selects both fields back (`profilechanges.go:39-43`). `RegistrationsPage.tsx:198-200` prints the reason under a rejected badge and makes it mandatory (`:115-117`). Your own question — *which* states show a reason — is answered only by a code comment, so confirm the rule | Dashboard → التسجيلات, filter to مرفوض: the reason should sit in grey under each red badge. |
| E9 | No "تعديل الباسوورد" in the user edit page | **FIXED** | `06ab6c0`. `UsersPage.tsx:53` adds a password field ("Leave blank to keep unchanged"), routed to `POST /api/admin/users/:id/password` (`admin_status.go:1223-1264`) | Dashboard → المستخدمون → تعديل, scroll to the bottom: a "New password" box. |
| E10 | التسجيلات has no details and no export | **FIXED** | `af3e8c9`. `RegistrationsPage.tsx:140-205` renders name, phone, DOB, address, role, date, status and reason; the row menu's first item is عرض (`:213`); `ExportCsvButton` at `:271-277` with 11 columns | Dashboard → التسجيلات: a تصدير button top-right, and ⋯ → عرض opens the applicant's full record. |
| E11 | Delivery needs إيقاف / أرشفة | **FIXED** | `06ab6c0`. `DonationsPage.tsx:45` lists 8 delivery states including `suspended`, `paused`, `archived`, exposed inline at `:274-283`; server allowlist matches (`admin_status.go:413`) | Dashboard → التبرعات: click the delivery dropdown on any row — إيقاف and أرشفة are in the list. |
| E12 | Partner images do not appear in the table | **CANNOT VERIFY** | Rendering code is correct end to end (`PartnersPage.tsx:167-172`, `listings.go:85`, static route `main.go:307`, uploads `admin_upload.go:140`). What blocks me: the dashboard session expired, so I cannot see what `logo_path` actually holds for existing rows. The symptom you describe is the browser's broken-image glyph, which points at stored paths that do not resolve — legacy seeded rows (`001_full_v2.sql:1118`), not missing code | Dashboard → الشركاء: right-click a broken logo box → "Open image in new tab". A 404 means the file is missing or the saved path is wrong; the logo means the dashboard is fine. |
| E13 | Last-actions column has no عرض; cannot see who added it | **FIXED** | `0287b0c` + `af3e8c9`. `CityGuidePage.tsx:280-283` adds an actions column with View first; `submitted_by` is written on user submissions (`listings.go:493`, migration 038) and `DetailPage.tsx:118-124` resolves any `_by` column to `"Name (#18)"`. Caveat: entries created **from the dashboard** set no `submitted_by`, so those show blank | Dashboard → دليل المدينة → ⋯ → عرض on a user-submitted entry: "مُقدَّم من" shows a person's name. |
| E14 | حقل التقديم layout breaks when dropdown options change | **FIXED** | `06ab6c0`. `StatusCell.tsx:97-113` computes the width from the **widest** option, not the selected one, and pins it — the comment states the intent. The signups status column is additionally pinned at 180px (`VolunteersPage.tsx:914`) | Dashboard → المتطوعين → تسجيلات المهام: change a status from a short word to a long one; the columns beside it must not shift. |
| E15 | Need a fixed list always showing موافق/قبول/رفض/تراجع/حذف | **STILL BROKEN (حذف missing)** | Four of five are permanently available: the status dropdown always offers all 8 states (`VolunteersPage.tsx:918-923`) and the server validates against a flat allowlist, so قبول/رفض/تراجع work from any state. **حذف does not exist** — `main.go:713-716` registers only GET and two POST routes for `volunteer_mission_signups`, no DELETE, and no delete control appears | Dashboard → المتطوعين → تسجيلات المهام → ⋯ on a completed row: you can still change its status, but there is no حذف anywhere. |
| E16 | Login must accept international numbers | **FIXED** | `d19e213` + `5ba891a`. **Verified live**: the sign-in screen shows an Iraq flag and `+964` with a dropdown arrow; `login.dart:290-342` mounts a country picker ("200+ available") driving normalization at `:199-202`, with Iraq-strict vs generic 4–14-digit validation at `:113-123`; the server accepts `+`/`00` prefixes (`auth/phone.go:52-59`) | App sign-in: tap the flag beside the phone box — a searchable country list opens. |
| E17 | Profile edits must not save until an employee approves | **FIXED (honesty) — scope NEEDS OWNER** | **Honesty half fixed.** The app now reads the `pending_review` list the server has been sending since migration 093 (the handler comment at `profile.go:325-332` says it exists "so the app can say 'waiting for approval'"; nothing read it). The message is picked by `profileSaveMessage()` in `api/profile_api.dart` from what the server actually did — four outcomes, tested. A second, quieter version of the same lie is fixed with it: a photo awaiting review used to be cached locally and shown as the user's avatar everywhere, so the app displayed a picture no one had approved. **Scope half is your decision, not a defect** — only `full_name` and `profile_picture` are reviewable (`profilechanges.go:17-20`). `address` still saves instantly, and photo **removal** still skips review. Extending review to those needs a migration and a dashboard queue change, and it is a policy call: see "Needs your decision" | App → تعديل الملف الشخصي: change your name **and** your address, save. It should now say "تم الحفظ — بانتظار الموافقة" and explain that the name is waiting while the rest is saved — matching what the dashboard queue actually holds. |
| E18 | All registrations and profile edits go through review | **FIXED** | `32fdd99` + `1bb6275`. Role-independent by construction: `registration.go:240` moves **any** submitting user to `pending`; `RequireApproved()` (`middleware.go:130-150`) 403s every non-approved user and is mounted on the whole authenticated group (`main.go:462`). The app routes pending/rejected users to a waiting screen (`auth_navigation.dart:23-25`) | Register a fresh test account as متطوع: after the code step it should land on "waiting for approval" and refuse the home tabs until you approve it in التسجيلات. |

## F. Missing dashboard features

| ID | What the client reported | Verdict | Evidence | How YOU can check it |
|---|---|---|---|---|
| F1 | No dark/light mode toggle | **ALREADY OK** | `e3c3bce`. `ThemeToggle.tsx:26-52`, mounted at `AppShell.tsx:448`, persisted to localStorage — confirmed live: the browser still holds `theme=dark`. It is **an unlabelled sun/moon icon**, which is why you could not find it | Dashboard, top-right, between the sound icon and the language dropdown: a small sun/moon icon. Click it and the whole dashboard turns light. |
| F2 | No manual setting for auto-logout time | **ALREADY OK** | `SettingsPage.tsx:169-223` → `PUT /admin/settings/session-timeout` (`main.go:1050`, super-admin only, clamped 5–480 min at `admin_settings.go:139-156`); applied at `AppShell.tsx:186-222`. الصلاحيات additionally re-locks after 2 min idle (`IdleLock.tsx:14-19`) | Dashboard → إعدادات لوحة التحكم → مهلة الجلسة. Note the minimum is 5 minutes; your "2 minutes" applies only to locking the الصلاحيات section. |
| F3 | Unified fixed top action bar in every section | **STILL BROKEN (حفظ is dead)** | The bar renders on every page (`AppShell.tsx:465-469`) with رجوع/التالي/تحديث/حفظ (`TopActionBar.tsx:26-50`). Back/Next/Refresh work. **حفظ fires an `app:save` browser event that no page listens for** — searching all of `admin-web/src` finds it only at `TopActionBar.tsx:23` | Dashboard, any section: press **حفظ** in the top bar. Nothing happens, on any page. |
| F4 | Admin notification on every user operation, stored permanently | **ALREADY OK** | Every user create/role/tier/status/password/archive write appends to `app_events` (`admin_status.go:98,209,325,391,1215,1264,1342,1375`); shown at `DashboardPage.tsx:166`; deletable only by super-admin (`main.go:690`) | Dashboard → لوحة التحكم activity feed: every user add/edit/rank change is logged with who did it. Note it lives in the feed, not inside التقارير. |
| F5 | Export missing in six named sections | **ALREADY OK** | All six have it: `RegistrationsPage.tsx:271`, `MessagesPage.tsx:209`, `VolunteerBoardPage.tsx:231`, `NotificationsPage.tsx:133`, `PushNotificationsPage.tsx:289`, `ReportsPage.tsx:70` (plus 17 other pages) | Open التسجيلات، الرسائل، لوحة المتطوعين، الإشعارات، الإشعارات الفورية، التقارير — each has a تصدير ▾ offering CSV / Excel / PDF / Word. |
| F6 | No way to add a user manually | **ALREADY OK** | `UsersPage.tsx:565-567` → `handleCreate` `:516-537` → `POST /api/admin/users` (`admin_status.go:144-216`), created already-approved | Dashboard → المستخدمون: the green **مستخدم جديد** button top-right, beside the search box. |
| F7 | قائمة المهام is not editable | **STILL BROKEN (partial)** | Add/edit/delete/status all work (`MissionsPage.tsx:140-153, 250-263, 324-337`). Missing exactly what you asked beyond that: `volunteer_missions` has **no section/category column and no display_order** (`001_full_v2.sql:691-709`) | Dashboard → المهام: you can add, edit and delete a mission — but there is no way to put one in a section, and no arrows to reorder. |
| F8 | Dashboard-side project management (hide/disable/re-enable) | **ALREADY OK** | 3-state lifecycle active/hidden/finished (migration 004) editable at `CampaignsPage.tsx:45,255-260,345-347`; a finished campaign refuses new donations server-side (`donations.go:196-197`, code `campaign_finished`) | Dashboard → الحملات → تعديل → حالة: نشط / مخفي / منتهي. That covers switching off النفط الأبيض in summer and closing a met medical case. |
| F9 | Dynamic CMS admin panel, responsive | **ALREADY OK** | Sidebar reorder/regroup with no deploy (`SidebarLayoutEditor.tsx`, `main.go:1062-1063`); registration fields flipped required/optional/hidden from the dashboard (`main.go:970-972`); 8 catalogue CMS pages; responsive shell (`index.css:515`, `AppShell.tsx:110-113`) | إعدادات لوحة التحكم reorders the sidebar; قواعد الحقول turns registration fields on/off; open the dashboard on your phone — the sidebar becomes a drawer. |
| F10 | Verify beneficiaries against دائرة الرعاية | **NEEDS OWNER** | No integration exists and none can be built unilaterally. The only related data is a self-declared checkbox `registered_social_welfare` (migration 075:15) | Today the dashboard shows only what the applicant *said*. Verifying it needs a signed data agreement and an API from that department — your decision, not a code fix. |
| F11 | Reports exportable as Excel and Word | **ALREADY OK** | Excel (`.xls`) and Word (`.doc`) writers at `lib/csv.ts:87-125`, wired at `ExportCsvButton.tsx:94-95`; reports cover donations, volunteers, beneficiary cases, project requests and expenses (`ReportsPage.tsx:20-38`) | Dashboard → التقارير → تصدير ▾ → Excel or Word. Per-person rows export from المستخدمون / المستفيدون / المساهمات. |
| F12 | Comment moderation with a banned-words list | **ALREADY OK** | Admin-managed word list (`moderation/bannedwords.go`, `main.go:1016`) auto-holds matching comments as pending+flagged; approve/hide/delete at `CommentsPage.tsx:53-65` | Dashboard → الكلمات المحظورة to edit the list; → التعليقات shows flagged ones first with موافقة / إخفاء / حذف. |

## G. Dashboard — RTL direction & table alignment

| ID | What the client reported | Verdict | Evidence | How YOU can check it |
|---|---|---|---|---|
| G1 | Detail pages put labels far right, data far left | **STILL BROKEN** | One shared detail view for every section (`App.tsx:181` → `DetailPage.tsx:182-204`). The layout is a two-column CSS grid with a **fixed 220px label track**: `index.css:2089-2092` `.detail-grid { grid-template-columns: 220px 1fr }`. Neither `.detail-key` nor `.detail-value` sets `text-align`. Net effect: the label sits at the far right of a 220px track, the value starts ~234px in, and the rest of the row is empty. **Of the 25 `[dir="rtl"]` overrides in `index.css`, zero touch this block**, and it is byte-identical to the first snapshot | Dashboard in العربية → المستخدمون → عرض. Every field: Arabic label hugging the right edge, a wide empty band, then the value. That band is the defect. |
| G2 | Data breaks when the dashboard flips fully RTL | **STILL BROKEN (largely fixed; residue)** | Direction flips correctly (`i18n.tsx:99,103`); `Table.tsx:59-64` maps left/right → start/end; `formatId.ts:16` wraps ids in an LTR isolate. 26 logical-property usages and 25 RTL overrides in `index.css`. **Residue: 11 physical-direction inline styles across 6 files** — `MessagesPage.tsx:235`, `StaffChatPage.tsx:163,187`, `MarriageChatsPage.tsx:183`, `CaseVolunteerChatsPage.tsx:205`, `VolunteerBoardPage.tsx:532` all `textAlign:'left'` on conversation rows | Dashboard in العربية → الرسائل (also محادثة الموظفين): the names inside each conversation row sit on the wrong side compared with every other Arabic list. |
| G3 | التبرعات headers do not line up with the data | **FIXED** | `06ab6c0` + `2a87ebc`. Headers and cells are generated from **one** array (`Table.tsx:84-88` and `:170-178`), so a mismatch is structurally impossible. The original cause is documented at `Table.tsx:140-151`: a CSS transform on `<tr>` pulled rows out of the table's column tracking; the diff removed `y: 6` from the row animation | Dashboard in العربية → التبرعات: المعرف، المجموع، المتبرع each sit under their own heading, and stay aligned after paging. |
| G4 | المستفيدون table misalignment; المعرف empty | **FIXED** | Same single-source proof; columns at `BeneficiaryPage.tsx:373` → `:533-537`. The المعرف cell is populated (`:374`), and the checkbox header and cell are gated on the same `selectable` prop, so a one-column shift cannot occur | Dashboard in العربية → المستفيدون: المعرف shows `#T…` values and المتبرع/العنوان are not shifted across. |
| G5 | السوق table misalignment | **FIXED** | Same proof; `MarketplacePage.tsx:270` → `:365-371`. The reported symptom is exactly the one-column body shift fixed in `06ab6c0` | Dashboard in العربية → السوق: tick-box, image and category each under their own heading. |
| G6 | الكفالات top header row is irregular | **FIXED** | Same proof; `SponsorshipsPage.tsx:187` → `:314-317` | Dashboard in العربية → الكفالات: read the heading row against the first data row. |
| G7 | Same RTL fix needed on المستخدمون → عرض and دليل المدينة → عرض | **STILL BROKEN** | Inherits G1 — both عرض links go to the **same** component (`UsersPage.tsx:411`, `CityGuidePage.tsx:282`). There is no second detail page | Dashboard in العربية: المستخدمون → عرض, then دليل المدينة → عرض. Same wide gap on both. |
| G8 | User detail field order follows the English layout | **STILL BROKEN** | Three sub-claims. RTL flip **works**; labels **are** translated (`DetailPage.tsx:185` → `fieldLabel`). But **field order is not implemented at all**: `DetailPage.tsx:183` renders `Object.entries(resp.item)`, i.e. whatever order the database returns from `SELECT *` (`admin_detail.go:88`). There is no designed order in any language, and no per-locale reordering — plus the G1 gap | Dashboard → المستخدمون → عرض in العربية, then in English: the field order is identical in both, and it is database order, not a designed one. |

## H. Dashboard — permissions, sessions, audit & security

| ID | What the client reported | Verdict | Evidence | How YOU can check it |
|---|---|---|---|---|
| H1 | New إدارة الصلاحيات section, admin only, with 2FA | **STILL BROKEN (partial)** | The section exists and is super-admin-only (`navLayout.ts:74`, `main.go:928-939`), and every permission change needs PIN + single-use SMS OTP (`admin_permissions.go:134-174`). But 2FA **on login** is env-gated and **off by default** — `auth.go:337-343` reads `ADMIN_LOGIN_2FA`, unset = password only | Sidebar → الصلاحيات (only you see it), flip any checkbox: it asks for your password then an SMS code. Now log out and back in: only the password. That half is off until the flag is set. |
| H2 | Choose which menus each user may enter, hide the rest | **ALREADY OK** | `AppShell.tsx:281-286` hides any menu whose module lacks `view`; per-tier (migration 015) and per-employee (migration 055, `permissions.go:209-225`) | الصلاحيات → untick `view` for المستفيدون on the موظف row, then sign in as an employee: the menu item is gone entirely. |
| H3 | Per-section precision: view/edit/archive/delete/add | **ALREADY OK** | Exactly those actions plus export: `permissions.go:39-46`, `:145`; grid at `PermissionsPage.tsx:334-364` | الصلاحيات: each section row has one checkbox per operation — اطلاع / إضافة / تعديل / أرشفة / حذف / تصدير. |
| H4 | Full show/hide per role | **ALREADY OK** | 24 module slugs matching the sidebar (`permissions.go:134-142`); 5 tiers (`permissions.go:30-36`) | الصلاحيات shows every sidebar section against every rank; unticking اطلاع hides it for that rank. |
| H5 | Grant or block export/download per interface | **ALREADY OK** | `ActionExport` (`permissions.go:45`); `ExportCsvButton.tsx:46,56` hides the whole CSV/Excel/PDF/Word menu when the bit is off | Untick تصدير for المساهمات on a rank, then open المساهمات as that user — no تصدير button. |
| H6 | Detailed per-section add/edit/delete/archive control | **ALREADY OK** | Overrides stored per tier (migration 015) and per user (migration 055); resolved at `permissions.go:102-130`, `:209-225` | الصلاحيات → pick a specific employee from the dropdown to give one person different rights from their rank. |
| H7 | Grant or block financial reports, statistics, audit logs | **ALREADY OK** | `reports` and `audit` are modules (`permissions.go:137-138`); the audit endpoint is gated `perm("audit","view")` at `main.go:738` | Untick اطلاع on التقارير and سجلات التدقيق for a rank — both vanish from that user's sidebar. |
| H8 | Control activating/deactivating accounts, products, stores | **STILL BROKEN (partial)** | Products/stores/partners/media status **are** matrix-controlled (`main.go:797,800,801`). Account enable/disable is **not delegable** — `main.go:831` `account_status` is hard-pinned to `RequireSuperAdmin()`, so no checkbox can grant it | الصلاحيات lets a rank enable/disable products and stores, but there is no checkbox anywhere letting anyone but you suspend a **user account**. |
| H9 | Control who may change product status | **ALREADY OK** | `main.go:797` gated by `perm("marketplace","edit")`; allowed values at `admin_status.go:400` | Untick تعديل on السوق for a rank — that user can no longer set a product to مقبول/مسودة/مرفوض. |
| H10 | Control who may see phone and email; otherwise encrypted | **STILL BROKEN (partial)** | The **detail page** redacts server-side (`admin_detail.go:152-157`), but the **list** does not: `UsersPage.tsx:303` masks in the browser only (`lib/permissions.ts:38-52`) while `/api/admin/users` still returns the real phone in the response. Nothing is encrypted at rest | In المستخدمون a restricted employee sees `••••61` — that is cosmetic; the real number still travels to their browser and is visible in the page's network data. |
| H11 | Force logout the instant permissions are reduced | **STILL BROKEN (partial)** | Tier demotion, suspend, ban and archive **do** force-logout (`admin_status.go:95-97, 322-324, 388-390`; revoked tokens refused at `token.go:177-179`). A checkbox change in الصلاحيات does **not** (`admin_permissions.go:179-189, 369-375`) — it takes effect on their next click instead | Suspend a user in المستخدمون: kicked out instantly. Untick a permission in الصلاحيات: they stay logged in and just get "لا تملك صلاحية" next time. |
| H12 | Audit log of every permission change, undeletable | **ALREADY OK** | `permission_audit_log` (migration 015:44-56) with a tamper-evident hash chain (migration 024; `permissions.go:295-372, 442-487`). Stores actor, time, target, old→new, IP. **No delete endpoint exists anywhere** | Scroll to the bottom of الصلاحيات: a read-only table of who changed what, when and from which IP. No delete button, by design. |
| H13 | Super Admin protection — nobody may edit that account | **FIXED** | Was real, and was a working takeover: `PATCH /api/admin/users/:id` (`main.go:876`, `perm users.edit`) never called the guard the other eight write paths use, and a test driving the real request chain rewrote a `super_admin`'s phone as an `admin`, as an `employee` and as a `supervisor`. One shared guard now enforces a **tier floor** (never write to an account that outranks you) plus a **credential rule** (changing a *staff* phone is Super-Admin-only — the number is where a sign-in code lands, and **one super_admin and one admin currently have no password at all**, so for them it is sufficient on its own) — `internal/handlers/admin_user_guard.go`, called from `admin_edit.go` and `admin_status.go`. Correcting an ordinary user's number still works for any rank with تعديل | In المستخدمون an ادمن now gets "هذا الحساب يملك مستوى صلاحية أعلى من مستواك" instead of a saved phone change on your row. Fixing a beneficiary's typo'd number is unchanged. |
| H14 | Temporary block + auto-logout on rapid changes, SMS to lift | **STILL BROKEN** | The only guard is a one-minute 429 throttle, disabled unless `PERM_CHANGE_MAX_PER_MIN` is set (`admin_permissions_ratelimit.go:19-60`). No block, no auto-logout, no SMS-to-lift | Change permissions rapidly in الصلاحيات — nothing blocks the account and no SMS unlock exists. |
| H15 | Global trash; final delete only by admin with password | **FIXED (29 of 31), one gap left** | Confirmed by counting the routes: 31 admin DELETEs, 16 recoverable, **15 permanent** — including the client's two examples. Root cause was structural: the trash helper was a private method on one handler, unreachable from the catalogue/task/comment handlers. It is a shared `trashRow()` now and **13 of the 15 were converted** (all categories, payment methods, professions, marriage packages, tasks, comments), with `restorableTables` extended in the same change so they can come back out. **2 stay permanent by decision:** `app_events` (an append-only log, already Super-Admin-only) and `banned_words` (its blocklist is cached in-process with no TTL and only its own store refreshes it — bypassing that would keep enforcing a word you deleted). Still not built: "archive instead of delete" for non-user resources | Delete a **فئات المشاريع** or **طرق الدفع** row → it now appears in المهملات with its Arabic name, and Restore brings it back. |
| H16 | CSV export gated, password required | **ALREADY OK** | `ExportCsvButton.tsx:58-71` prompts for the password and verifies it via `POST /admin/verify-password` (`admin_status.go:1275-1307`) before any download; the button only renders when the export bit is granted | Press تصدير on any section: it asks for your password first. Caveat: the file is built in the browser, so that PIN is a dashboard gate, not a server lock. |
| H17 | JSON DB export restricted to the super admin | **ALREADY OK** | `main.go:923` → `RequireSuperAdmin()`; the handler re-checks the caller's own password (`admin_export.go:112-145`); button rendered only for super-admin (`AppShell.tsx:374-384`). Delegating it to others is not built | The amber **تصدير قاعدة البيانات** button at the bottom of the sidebar appears only on your account and asks for your password. |
| H18 | Force Logout — end any user's session immediately | **ALREADY OK** | `main.go:830` + `admin_status.go:257-276` revoke every token row; `token.go:177-179` refuses a revoked token on the very next request, from app or dashboard | المستخدمون → الإجراءات → إنهاء الجلسة: the user's app and dashboard both demand a fresh login on their next tap. |
| H19 | Force actions for any rank of user | **ALREADY OK** | Force logout (`main.go:830`), suspend/ban (`main.go:831`, `admin_status.go:287-327`), delete-to-trash (`main.go:902`). None inspect the target's rank — only a Super-Admin target is shielded | In المستخدمون the same الإجراءات menu works on عادي، متطوع، مشرف and مساهم alike. |
| H20 | Main-admin change confirmed via BOTH phone and email | **STILL BROKEN** | Phone-side protection exists, but there is **no email-sending capability in the backend at all** — no SMTP client, no mail provider, nothing (searched all of `backend/`). Only OTPIQ SMS exists (`auth/otpiq.go`) | No email code is ever sent for anything in this system today; the double confirmation cannot happen without adding an email provider first. |
| H21 | Require a password when changing user roles | **ALREADY OK** | `UsersPage.tsx:331` (نوع المستخدم) and `:359` (صلاحية الوصول) both call `verifyPin()` → `/admin/verify-password` before saving. Caveat: enforced in the dashboard only — `main.go:810`/`:822` accept the change without a password | Dashboard → المستخدمون, change anyone's role or rank: it demands your password before saving. |
| H22 | Progressive OTP lockout: 2h → 6h → 1 day | **ALREADY OK** | OTP abuse per phone is **exactly 2h → 6h → 24h** (`auth/otp_lock.go:23-29`), triggered by >5 OTP requests in 15 minutes (`:19-20`), enforced at `auth.go:608-641`. **Note the different number you may notice:** the separate wrong-*password* lockout is 15 min → 1 h → 24 h (`auth/login_lock.go:30-36`) | Request more than 5 SMS codes on one number within 15 minutes: locked 2 hours, then 6, then a day. A wrong *password* 5 times locks 15 minutes first. |
| H23 | Auto-generated identity codes instead of real names | **STILL BROKEN (partial)** | Codes exist for recipients (`ER-%06d`, `registration.go:162,211`), volunteers (`VL-%06d`, `:479-490`) and marriage profiles (`marriage.go:245-283`), searchable by staff since `d0b19be`. **Donors get no code** — only a manual alias. Dashboard lists still display `full_name` first | Search a مستحق or متطوع by code (ER-000042 / VL-000042) in المستخدمون — it works. A مانح has no such code, and every list still shows real names by default. |

## I. Global terminology & naming

> Reconciled in [`TERMINOLOGY.md`](TERMINOLOGY.md). The rows below **re-verify that
> document against the actual locale files** rather than trusting it — two of its claims
> turned out to be stale.

| ID | What the client reported | Verdict | Evidence | How YOU can check it |
|---|---|---|---|---|
| I1 | التبرعات → "Back Us" / Contributions المساهمات | **NEEDS OWNER** | The **label** rename largely landed: `ar.ts` مساهم ×28 vs تبرع ×9; app مساهم ×65 vs تبرع ×14; `en.ts` Contribution ×26 vs Donation ×22. But **"Back Us" / "ادعمنا" = 0 occurrences** — never applied. API and DB identifiers are untouched (the highest blast radius) | Decide first: is the giving section called **المساهمات** or **ادعمنا**? Nothing more should be renamed until you answer. |
| I2 | المستفيد → Recipients / المستحقين | **STILL BROKEN (dashboard Arabic)** | App is done: مستحق ×20 / المستحق ×16, مستفيد only ×3. **Dashboard is not**: `ar.ts` مستفيد ×20 / المستفيد ×15 vs مستحق ×1. The English word is a live conflict — `CampaignsPage.tsx:56` ships "Eligibles" while the role labels ship "Recipient" | Compare app and dashboard side by side in العربية: the app says "مستحق", the dashboard still says "المستفيدون". They must match. |
| I3 | الكفالات → Assistance / المعونات | **NEEDS OWNER** | English half done: `en.ts:41` `sponsorships: 'Assistance'` ("Assistance" ×9). **Arabic half not**: `ar.ts:41` still `'الكفالات'`, and **المعونات = 0 occurrences** anywhere. Note `TERMINOLOGY.md` is now stale here — it records "الدعم", but `98629a7` (committed after that doc) deliberately set it back to الكفالات to resolve a collision | Decide: **الكفالات** or **المعونات**? The English already says "Assistance"; the Arabic is waiting on you. |
| I4 | المتطوعين must stay unchanged | **ALREADY OK** | Nothing was renamed. `ar.ts` متطوع ×26, `en.ts` Volunteer ×21, app متطوع ×20 / Volunteer ×75 | Dashboard and app in العربية: the section still reads "المتطوعون". |
| I5 | حساب المتبرع → مانح | **ALREADY OK** | مانح ×16 dashboard / ×19 app. **Confirmed live**: the app's home header reads "لوحة المانح". Residual المتبرع is 4 descriptive sentences in the dashboard and 1 verb form in the app ("إجمالي المتبرع به") — not the account name | App in العربية → your profile: the account is labelled "مانح". |
| I6 | متعفف → مستحق | **ALREADY OK** | **`متعفف` = 0 occurrences** repo-wide across `.ts`/`.tsx`/`.dart`. Replacement مستحق present ×20 in the app | Search the app in العربية for "متعفف" — it appears nowhere; you will see "مستحق". |

## J. App — home screen & navigation restructure

| ID | What the client reported | Verdict | Evidence | How YOU can check it |
|---|---|---|---|---|
| J1 | Guest-login button was never added | **STILL BROKEN (2 of 3 fields)** | The button **does exist** — `login.dart:553-573`, **verified live** ("الدخول كزائر" on the sign-in screen); `37c47c0` did not remove it. But the form it opens asks only **Username** (`:800-822`) and **Password** (`:824-855`) — **no الاسم field**. Confirmed server-side: `guestCredentialsReq{Username, Password}` (`auth.go:896-900`) | App → sign-in screen → "الدخول كزائر" at the bottom: the form asks only for a username and a password; there is nowhere to type your name. |
| J2 | Guest scope restrictions | **ALREADY OK** | Enforced on the server, not just hidden: `RequireNotGuest` blocks requests, purchases and messaging on **30 routes** (`middleware.go:160-179`); City Directory is a hard block (`BlockGuestOptional`). Browsing scope is dashboard-controlled across 10 screens (`guest/guest.go:31-44`), and the client **fails closed** (`guest_session.dart:65-67`) | Dashboard → Guest Access: switch a section off and reopen as a guest — it disappears; then try to submit any request as a guest and you are told a full account is required. |
| J3 | Animated slider banner with three counts | **ALREADY OK** | `impact_stats_slider.dart`: `PageView.builder` auto-rotating every 4s (`:25,84,93-97`), counting up (`:260`), dot indicator (`:160-176`); the three metrics at `:53-78`. **Verified live** — it rotated from "المستفيدون المؤهلون 5" to "المانحون 20". Likely why you thought it missing: each metric is dropped at zero and the whole banner hides on error (`:114`) | App → Home, the "أثرنا" band: it should cycle every ~4 seconds between donor count, beneficiary count and completed works. |
| J4 | Welcome card: name on the same line; delete "ابق مستعد" | **ALREADY OK** | **Verified live**: "أهلا بعودتك، dsfgnhjgfds" on one line, no second line. `dashboard.dart:170-179` is a single `Text` with the name interpolated; **"ابق مستعد" has 0 occurrences in any `.dart` file** | App → Home, welcome card: one line, "أهلا بعودتك، [your name]", nothing underneath. |
| J5 | The three home cards in one rectangle, evenly divided | **ALREADY OK** | `dashboard.dart:1428-1487` is one panel wrapping one `Row` of three `Expanded` children. **Verified live** | App → Home: منتجاتنا / مشاركتي / دليل المدينة sit in one rounded box split into three equal parts. |
| J6 | Profile icon top-right opening an 11-item menu | **STILL BROKEN (10 of 11)** | The circular avatar is done (`dashboard_screen.dart:612-655`, `:511`) and **verified live**. The menu is 9 exact + 1 partial + 1 missing: **اللعبة is entirely absent** — the wheel/coupon screens exist but their only door is the donor Home quick-actions panel; and **الاشعارات is an on/off switch, not the list** (`:153`). All confirmed live | App → tap the round photo top-right: you should see all 11 entries. اللعبة is missing, and tapping الاشعارات flips a switch instead of opening your notifications. |
| J7 | A Menu button on every page and a Back button on every page | **STILL BROKEN** | Back was largely unified by `ec76078`, but **12 files still use a raw `AppBar(`** and never picked up the shared chrome (incl. `donations_screen.dart`, `chat_conversation_screen.dart`, `edit_profile.dart`). For the menu: `openDrawer|drawer:` → **0 hits**; `Drawer(|endDrawer` → **0 hits**. There is no hamburger anywhere in the app | Open any inner page and look at the top corners: a back arrow on most pages, but never a menu button to jump to the main menu. |
| J8 | In-app search in every sub-section and list | **STILL BROKEN** | Global search exists and works across 5 tables (`search/search.go`, `global_search_screen.dart`) but is reachable from only **2 places** — the top bar and the profile screen. Of 67 screen files, exactly **one** list has its own in-list search (`marriage_search_screen.dart`) | App: the magnifier in the top bar searches everything — but open المساهمات, أعمالنا, الكفالات or الإشعارات and none has a search box. |
| J9 | Add a support button at the top of the app | **STILL BROKEN** | The top bar has exactly four trailing controls — search, bell, messages, avatar (`dashboard_screen.dart:484-511`), **verified live**. A support button was **deliberately removed**, documented at `dashboard.dart:794-798` ("support is reachable from Settings"), attributed to `e634261` | App: look along the top bar — magnifier, bell, message icon, your photo. No help icon; support is two taps deep in the profile menu. |
| J10 | Add الشروط والأحكام | **FIXED** | `humanitarian/lib/modules/legal/`. **Verified live**: "الشروط والأحكام" is in the profile menu between "اتصل بنا" and "تسجيل الخروج" | App → photo top-right → scroll down → الشروط والأحكام. |
| J11 | Pressing "سجلني" surfaces the beneficiary request | **NEEDS OWNER** | The string **"سجلني" does not exist in any source file** — searched all of `humanitarian/`, `admin-web/` and `backend/`; "register me" / "sign me up" also return zero. There is nothing to test against. The nearest controls are the beneficiary home's "إرسال الطلب" (`dashboard.dart:467`) and "طلباتي" (`:548`) | There is no button labelled "سجلني". Tell us which button on which screen you meant, and whether the beneficiary request appearing is the bug or the behaviour you wanted. |

## K. App — missing sections & features

| ID | What the client reported | Verdict | Evidence | How YOU can check it |
|---|---|---|---|---|
| K1 | "أعمالنا" section was never added | **FIXED** | `e634261`. `our_work_screen.dart:17`, opened from `profile_menu_screen.dart:112`; full-post view at `:322`. **Verified live** — "أعمالنا" is in the profile menu | App → photo top-right → أعمالنا: a two-column grid of the organization's projects. |
| K2 | أعمالنا content model — 16 named categories in two groups | **FIXED** | `dd44fd1` / migration `088_our_work_activities.sql:24` adds `group_key`; `:36-52` seeds **all 16** categories you named, split into المساعدات الإنسانية and برامج أخرى; rendered at `our_work_screen.dart:104-169`; new categories are admin CRUD | Inside أعمالنا, look at the filter chips: grouped under "المساعدات الإنسانية" (كفالة يتيم، أرملة، السلال، الملابس، الطبية، الموسمية) and "برامج أخرى" (الطفولة، المرأة، سبل العيش، البيئة، التراث، السلام، المعارض، التعليم، المجتمعية). |
| K3 | Per-post model + Activity Code + interactions | **FIXED** | `dd44fd1`. Fields at `news_activities_screen.dart:291-400`; like/comment/share/save bar at `:404-475`; activity-code pill at `:313,349`; **auto code per category** at `mediacategories.go:182-202` + migration 089 — a new category gets its own prefix with no code change | Open any post in أعمالنا: the date, place, a code chip like `HUM-000123`, and four buttons — إعجاب، تعليق، مشاركة، حفظ. |
| K4 | عجلة الحظ / لعبة was never added | **FIXED** | `e634261`. `wheel_of_fortune_screen.dart:13` and `lucky_coupon_screen.dart`, wired into the Home quick-actions panel (`dashboard.dart:347,358`). Caveat: challenge names are a hardcoded Dart list (`data/motivational_tasks.dart:9`), so renaming one needs a developer | App → Home → الإجراءات السريعة → عجلة الحظ. Spin it and a challenge pops up. |
| K5 | Colour-coded operation status with numbers | **STILL BROKEN** | The badge itself is correct (`operation_status_badge.dart:11` — green 100%, red 0%, orange partial, percent inside). But it is rendered in only 2 places and both feed it **money raised ÷ goal** (`featured_campaigns.dart:249`) while the label says "تم التسليم بالكامل / لم يُستلم بعد". **No delivery-based status exists** on donations, entitlements or receipts | App → Home, the coloured circle on a campaign card shows how much **money** was collected, not how much aid reached the family — so a green 100% can still mean nothing was delivered. |
| K6 | Dedicated الشركاء section with profile and 1–5 rating | **FIXED** | `c86fcb9` + `partnerratings`. `partner_detail_screen.dart:86-232` carries name, logo, website, type, location, phone, email, social links; 1–5 stars at `partners_screen.dart:448-560` backed by `partnerratings.go:28`; the organization's own assessment with all four criteria at `:305-370`; joint activities at `:234-300` | App → الإعدادات → شركاؤنا → tap a partner: logo, contact details, a row of stars you can tap, and a list of joint activities. |
| K7 | Settings: 4 languages + per-type notification control | **STILL BROKEN (per-type half)** | The four languages are done (`settings_section.dart:441-459`), **verified live**. Notifications is a **single global on/off** (`:717` → `users.notifications_enabled`); no per-type control exists in the app, the API or the database — `notify.Send` gates everything all-or-nothing (`notify.go:96`) | App → photo top-right: اللغة gives all four languages, but the الإشعارات switch beneath it is one on/off — you cannot choose which kinds of alerts you get. |
| K8 | Privacy settings: choose which fields others see | **STILL BROKEN (2 gaps)** | The page works (`field_privacy_screen.dart:39`, reached via الإعدادات → إعدادات التحكم → الخصوصية) and options are served from the database so staff can add more without a release. But **البريد الإلكتروني is not among the 10 seeded options** (migration `083:29-40`), and nothing varies the option set by account type as you asked | App → الإعدادات → إعدادات التحكم والتفضيلات → الخصوصية: switches for name, phone, gender, address, birth date, photo, age, education, job, governorate — **no email switch**. |
| K9 | سجل النشاط page | **FIXED** | `role_history_screen.dart:11` with filters and a per-record detail sheet (`:120-170`); the backend aggregates donations, sponsorships, cases, project requests, volunteer applications and signups (`history/history.go:51-95`). Two caveats: the list is role-scoped (a donor sees donations + sponsorships only), and it opens from **Home**, not the profile page as you asked | App → Home → "سجلي": a filterable list of everything on your account; tapping a row opens its details. |
| K10 | Technical support with status, replies, WhatsApp escalation | **FIXED — reply half not yet released** | `225400d` built the screen (compose, ticket list with status pill, staff reply inline at `technical_support_screen.dart:474-500`, WhatsApp after tickets on >3 separate days at `:39,182-183` + `support.go:69-76`). `3ba3fce`/`0e4bf5a` complete it but are **not deployed** — see A3 | App → photo top-right → الدعم الفني: send a message, it appears in a list with a status. Staff replies will show under your message once the next release ships. |
| K11 | "تفريغ المساحة" clearing local data only | **FIXED** | `settings_section.dart:122-127` → `_clearCache` at `:167-206`: confirms first, clears the image cache and temp folder, deliberately leaves the session alone; the confirmation says "سيبقى حسابك مسجّلاً". **Verified live** — "مسح الذاكرة المؤقتة" is the last row of the الإعدادات tab | App → الإعدادات tab → مسح الذاكرة المؤقتة: a box confirms and tells you your account stays signed in. |
| K12 | من نحن: three named sub-sections, extendable | **STILL BROKEN** | "About Us" is **one free-text blob**: `content_page_screen.dart:90-118` renders only `title` + `body`, and the `app_content` table has no other columns (migration 025:5-17). The three sub-sections do not exist as fields, and the shipped text is a single sentence | App → photo top-right → من نحن: one paragraph, not three separate sections about the app, the organization and its goals. |
| K13 | تواصل معنا: logo, phone, WhatsApp, email, socials, address | **STILL BROKEN** | Same single free-text page. **None** of logo, phone, WhatsApp, email, social links or address/map exist as fields; nothing is tappable and there is no map. The shipped Contact text contains no phone number or email at all (migration `041:11-16`) | App → photo top-right → اتصل بنا: one sentence telling you to reach out — no number to tap, no email, no socials, no address. |
| K14 | خطوبتي feature set | **STILL BROKEN (profile self-management)** | Built: filtered search incl. staff-configurable criteria (`marriage_search_screen.dart:18-22,105`), save-a-person (`:126-133`), **all three** contact-request types (`marriage_request_sheet.dart:37-51`, `e10d8ae`), subscription with payment methods. **Missing:** the user cannot edit, pause or delete their own marriage profile — the screen offers only "submit a new one" (`marriage_my_profile_screen.dart:96-135`); deletion is admin-only | App → الفعاليات (marriage) tab: search, filters, bookmark and the three request options all work — but open "ملفي" and there is no تعديل, no إيقاف and no حذف. |
| K15 | Products: 10 categories, 6 functional labels, 6 per-product fields | **STILL BROKEN** | All 10 spec categories seeded (migration `099:53-62`) **but 12 older generic rows were left in place → 22 categories show**. Per-product 5 of 6 (الحالة/التوفر has no field). **Functional labels: 0 of 6** — `ListProducts` takes no filter args (`marketplace.go:76`, `ORDER BY id DESC`) and the app sends only page+limit. **`brand` is dead in three places**: the column exists and the admin shows an input, but it is absent from the edit request, the create INSERT, and both SELECTs | App → منتجاتنا: no filter, sort, category tabs, "الأكثر مبيعاً", "وصل حديثاً", "العروض" or "العلامات التجارية". Then type a Brand on any product in the dashboard, save, reopen it — the Brand is empty. Also count the categories: 22, not 10. |
| K16 | دليل المدينة: 6 sectors with sub-categories | **STILL BROKEN (sub-categories unreachable)** | **6 sectors are seeded** (migration 052 + `100:29-33`, `6293696`) — none of your six is missing; **confirmed live**, the filter chips are there. But 27 sub-categories exist (migration `101:34-68`) and **the app never fetches them** — only `citySectors()` is exposed; grepping the app for `city-categories` returns zero. Entries still store a free-text `category`. Two labels differ from your wording (`government` = "الدوائر الحكومية والسيادية", `education` = "القطاع التعليمي والأكاديمي") | App → دليل المدينة: count the sector chips — 6. Tap into one: no sub-categories underneath. When you add a business, "التصنيف" is a typing box, not a list. |
| K17 | Per-place fields incl. direct call, hours, socials | **STILL BROKEN (3 partial)** | Present and working: name, GPS + Google Maps link, **real direct dial** (`community_detail_screen.dart:161`, `tel:`), email, **genuinely computed مفتوح الآن/مغلق** (`_openNow()` `:582-616`), description, gallery. **Partial:** the address is **one free-text column** — no الحي/الشارع/أقرب نقطة دالة fields exist; **social links display but are inert** (`:230-235` renders plain non-tappable text, unlike `website`); sub-classification is free text | App → دليل المدينة → tap a place: the phone dials, "فتح في الخرائط" works, and a مفتوح الآن/مغلق badge shows. But the address is one line, and tapping the Facebook link does nothing. |
| K18 | "إضافة نشاط" for shop owners with admin approval | **FIXED** | `6f0a238` (re-audited in `08347a3`). `add_activity_screen.dart:12`, opened by a full-width button on the City Guide tab (`community_services_section.dart:321`) — **verified live**, the blue "إضافة نشاط" button; saved as `status='pending'` (`listings.go:490-500`); approved/rejected at `admin_status.go:609` | App → دليل المدينة: the blue "إضافة نشاط" button at the top opens a form; what you submit stays hidden until an admin approves it. |
| K19 | Supervised donor↔beneficiary chat with data blocking | **STILL BROKEN** | A thread exists and staff can relay or claim it, but (a) it is approved by **the other party**, not an employee — `AcceptThread` explicitly rejects the initiator and requires the counterpart (`chat.go:159-181`); staff claiming is optional. (b) **There is no personal-data blocking** — the API returns the counterpart's real `full_name` and `phone` (`chat.go:66,267,411-436`) and the app prints the real name on every row and header. Masking exists only in the marriage chat | App → Messages → a donor↔family conversation: you see the other person's real name, and the chat opened because they accepted it, not because an employee approved it. |
| K20 | General chat for all user types + employee-only space | **STILL BROKEN** | Signed-in users get donor↔owner, support and case chats. Guests/visitors are **blocked** from messaging server-side. The confidential employee-only space exists **only in the web dashboard** (`staffchat/staffchat.go`, `StaffChatPage.tsx`) — nothing in the app references it | Messages works when signed in, but browse as a guest and messaging is blocked; the staff-only chat is in the dashboard, not the phone app. |
| K21 | Smart search by identity code for donation/support history | **STILL BROKEN** | `d0b19be` changed only the **staff user lookup** (`users.go:629-638`) — a dashboard feature. The app's search covers campaigns, media, products, partners and places only (`search.go:37-49`); there is no code lookup and no history readout by code anywhere in the app | App → the magnifier at the top → type an identity code: nothing comes back. That search only works for staff in the dashboard, and it finds the person, not their history. |
| K22 | Interactive map with pins; beneficiaries at ~500 m accuracy | **STILL BROKEN** | The map plots **City Guide places only** (`community_services_section.dart:691-740`). The ~500 m approximation is real but applies to `city_directory_entries.approx_location` (`listings.go:360-368`, 0.005° ≈ 500 m). **Beneficiaries have no coordinates in the schema at all**, and activities store a text location, not coordinates — so neither families nor events appear on any map | App → دليل المدينة: the map with pins is for shops and services. There is no map showing activities/events, and no family is placed on a map at all. |
| K23 | Share the app or any post to social media | **FIXED** | `6f0a238`. `core/app_share.dart:16` wired to the الإعدادات row (`settings_section.dart:105-109`) — **verified live**, "مشاركة التطبيق"; per-post sharing at `news_activities_screen.dart:522-533`. **One action for you:** `appShareUrl` is still empty (`api/links.dart:196`), so shares carry text with no download link | App → الإعدادات → مشاركة التطبيق, or the share button under any post. Note the shared message has no download link yet because the app's public URL has not been supplied. |
| K24 | 1–5 star rating app-wide | **STILL BROKEN** | Stars exist for **partners only** (`partners_screen.dart:448-560`, `partnerratings.go:28`). Nothing rates products, campaigns, places, services or volunteers; posts have only a like | You can tap stars on a partner's page and nowhere else — no stars on products, places, campaigns or services. |
| K25 | Dark mode in the app | **FIXED** | `5af3a74`. Tri-state System/Light/Dark picker (`app_theme_mode_picker.dart:35-40`), persisted, with a full dark palette (`tokens.dart:147-161`). **Verified live** — "الوضع الداكن" with داكن / فاتح / النظام in the profile menu | App → photo top-right → الوضع الداكن has three choices; pick داكن and the whole app turns dark instantly. |
| K26 | Full user control over sounds and vibration | **STILL BROKEN** | The mute control is fully implemented (`core/app_mute.dart:9`, UI at `profile.dart:1166-1213`) — but the screen holding it is referenced from **exactly one place in the app**: the AI assistant's 'profile' route (`bot_navigation.dart:58-63`). **Confirmed live**: neither the الإعدادات tab nor the profile menu contains any sound or vibration control | Go through الإعدادات and the profile menu: there is no sound or vibration switch anywhere. It exists but you can only reach it by asking the assistant to open your profile. |
| K27 | Push notifications the user can turn off | **FIXED** | Device registration at `core/push_registration.dart:34-90`; the toggle at `settings_section.dart:717`, shown in the profile menu (`:153`) — **verified live**; the backend honours it for both the in-app alert and the push (`notify.go:92-98`) | App → photo top-right → switch الإشعارات off: you stop getting both the bell alerts and the phone pop-ups. |
| K28 | AI chatbot + an AI icon beside each menu | **STILL BROKEN (icon half)** | The chatbot is real and good — `bot_chat_screen.dart`, a 1,260-line four-language FAQ, an AI backend (`assistant/anthropic.go`), and it deep-links into the right screen. But it has **exactly one entry point**: a card inside الرسائل (`messages_screen.dart:664`) — **confirmed live** ("مساعد الدعم"). **No section screen carries an AI icon**; the shared page header has no assistant slot | App → the message icon at the top → the assistant card: the chatbot works. But no other menu or section has a small AI icon explaining that section. |
| K29 | Digital delivery confirmation with field photos | **STILL BROKEN** | Field photos and staff-recorded receipts exist and the recipient can view them (`aid_receipts_screen.dart:24`, migration 048) — **verified live**, "إيصالات المساعدات" in الإعدادات. But the screen is explicitly **read-only** (`:25`), there is no confirm button, and `aid_receipts` has **no `confirmed_at`/`confirmed_by` column and no confirm endpoint** | App → الإعدادات → إيصالات المساعدات: you can see what was delivered and the photos, but there is no button for the family to confirm they received it. |
| K30 | Save/bookmark any post into a saved box | **FIXED** | `0ce8304`. Save button on every post card (`news_activities_screen.dart:461-471`), a dedicated box at `saved_posts_screen.dart:16` opened from `profile_menu_screen.dart:120` — **verified live**, "تم الحفظ"; backed by a generic `saved_items` table | App → bookmark any post, then photo top-right → تم الحفظ. Tapping the bookmark again removes it. |
| K31 | Scope: Android + iOS, source handover, visual identity | **NEEDS OWNER** | Contract item, not a defect. Both platforms are configured (`ios/Runner.xcodeproj`, bundle `com.easytech.humanitarianApp`; `android/` with branded name and a full icon set). **One concrete blocker:** the Android `applicationId` is still the Flutter placeholder `com.example.humanitarian` (`android/app/build.gradle.kts:25`), which Google Play refuses — and it **cannot be changed after publication** | Nothing to test in the app. The one technical action needed is changing the Android package name off the "com.example" placeholder **before** the first Google Play upload. |

## L. App — registration & profile data specifications

> The client's framing — "the specified fields were never put in" — is **wrong for 16 of
> these 20 rows**. All 16 landed in commit `e634261`.

| ID | What the client reported | Verdict | Evidence | How YOU can check it |
|---|---|---|---|---|
| L1 | Donor — البيانات الأساسية (13 fields) | **FIXED** | `e634261`. **13 of 13** present: `registration_form.dart:1684-1917` + shared DOB `:1465` (day/month/year dropdowns), gender `:1582`, address `:1562`, occupation `:1624`. Governorate list = **18 entries** (`data/iraq_governorates.dart:5-24`). DB: migration `072:10-24` | App in العربية → sign up → choose **المانح**: national ID, four name boxes, اللقب, two phones, email, an 18-entry محافظة dropdown, التحصيل الدراسي, a GPS button, and تاريخ الميلاد as three dropdowns. |
| L2 | Donor — attachments, pseudonym, social links | **STILL BROKEN (3 of 4)** | Photos present (`:1894`, `:1904`). Real-name-vs-كنية present but on a different screen (`field_privacy_screen.dart:340-368`). **Social links absent for donors** — rendered only for beneficiaries (`:3377-3392`) and volunteers (`:4324-4346`), and the submit gate at `:1311-1323` is `(_roleId == 2 \|\| _roleId == 3)`, so a donor's links are never even sent | Register as **المانح** and scroll to the bottom: the two photo uploads are there, but **no** Facebook / Instagram / Telegram boxes. The nickname choice lives in الملف الشخصي → الخصوصية after signup. |
| L3 | Beneficiary — البيانات التعريفية (10 fields) | **FIXED** | `e634261`. **10 of 10**: `:1931-2100` incl. العشيرة `:1996` and رقم هاتف للطوارئ `:2082`. Auto code generated server-side as `ER-%06d` (`registration.go:130`) | Sign up as **مستحق**: the first panel shows all ten boxes, with a note that the كود تعريفي is created automatically. |
| L4 | Beneficiary — nationality / marital / legal dropdowns | **FIXED** | `e634261`. All 3 present with the **exact option counts** you listed: nationality 5 (`:2112-2140`), marital 7 (`:2141-2173`), legal 5 (`:2174-2205`) | On the **مستحق** form: الجنسية (5), الحالة الزوجية (7 — أعزب/خاطب/متزوج/منفصل/أرمل/مطلق/أخرى), الحالة القانونية (5). |
| L5 | Beneficiary — cascading محافظة → الجانب → الحي | **FIXED — data needs your sign-off** | `e634261`. Cascade works: `:2240-2300`; Nineveh opens الجانب `:2241`; the neighbourhood list is filtered by side at `:2288-2290`; other governorates get free-text `:2302`. **Two caveats:** `data/nineveh_neighborhoods.dart` holds 12 names per side and its own header calls them "a representative starting set, not an exhaustive official list"; and الجانب = **أخرى** silently shows the right-side list | On the **مستحق** form pick **نينوى**: الجانب appears, and الأيمن/الأيسر change the الحي list. Read the 24 neighbourhood names and tell us which real ones are missing. |
| L6 | Beneficiary — نوع السكن with rent reveal | **FIXED** | `e634261`. **6 of 6**: housing type has exactly the 6 options (`:2364-2393`); the **rent-amount field appears only for "إيجار"** (`:2394`); area `:2412`; floors dropdown with 4 options `:2429-2456`; rooms `:2457`; families `:2474` | On the **مستحق** form open نوع السكن (6 choices) and pick **إيجار** — a rent box appears; pick anything else and it disappears. |
| L7 | Beneficiary — education/employment with reveal | **FIXED** | `e634261`. **12 of 12**: `:2510-2712`, incl. **هل يعمل حالياً** `:2655` revealing مكان العمل `:2679` and الأجور `:2695` | On the **مستحق** form find هل يعمل حالياً؟ and choose **نعم** — مكان العمل and الأجور appear; choose **لا** and they vanish. |
| L8 | Beneficiary — social data incl. 6 age brackets | **FIXED** | `e634261`. **19 of 19**: `:2725-2910`, with all **6 age brackets** at `:2844-2875` | On the **مستحق** form scroll to البيانات الاجتماعية and count six age boxes: 0–5, 5–10, 10–15, 15–25, 25–40, 40 فما فوق. |
| L9 | Beneficiary — health data | **FIXED** | `e634261`. **10 of 10**: `:2927-3160`, incl. disability yes/no `:3020` revealing نوع الإعاقة `:3052`. Caveat: "شرح كل حالة مرضية" is **one** free-text box, not one per condition | On the **مستحق** form open البيانات الصحية; choose **نعم** for ذوي الاحتياجات الخاصة and نوع الإعاقة appears. Tell us if you need one explanation box per illness. |
| L10 | Beneficiary — attachments, possessions, needs | **STILL BROKEN (12 of 14)** | 8 attachment tiles at `:3182-3249` + furniture `:3273`, car `:3295`, needs `:3328`. **Missing for the beneficiary: بطاقة السكن and الجواز** — both exist, but only inside the volunteer block (`:4253`, `:4261`). The medical report is also one upload, not one per illness | On the **مستحق** form scroll to المرفقات and count the tiles — there are 8. **بطاقة السكن** and **الجواز** are not among them. |
| L11 | Beneficiary — privacy consent | **FIXED** | `e634261`. **2 of 2**: `:3432` (show the donor's real name) and `:3463` (share own data), both admin-hideable. Migration `077:18-19` | On the **مستحق** form, the last panel (الخصوصية): two yes/no dropdowns. |
| L12 | Volunteer — all option fields must be dropdowns | **FIXED** | `e634261`. **16 of 16**. All three "must be dropdowns" **are** dropdowns: DOB = 3 dropdowns (`:1477/1502/1527`), gender `:1588`, nationality `:3766`. اللغات is a **real multi-select**, 8 checkboxes (`:3789`). Auto code `VL-%06d` (`registration.go:481`) | Register as **متطوع**: تاريخ الميلاد is three dropdowns (not a calendar), الجنسية and الجنس are dropdowns, and اللغات lets you tick more than one of 8 at once. |
| L13 | Volunteer — housing and social, with القضاء | **FIXED** | `e634261`. **15 of 15**, including the extra **Nineveh → القضاء dropdown** (`:3847-3872`) before الجانب, and **عدد أفراد الأسرة as a dropdown** (`:4022`) | On the **متطوع** form pick **نينوى**: you get **القضاء**, then الجانب, then a side-filtered الحي. (Note عدد أفراد الأسرة is a dropdown here but still a typing box on the مستحق form.) |
| L14 | Volunteer — 8 attachments + social links | **FIXED** | `e634261`. All 8 at `:4212-4302` (المربع الذهبي، البطاقة الموحدة، التموينية، السكن، الجواز، صورة رسمية، شهادة التخرج، CV) + 4 link boxes at `:4313-4365`. Migration 078 | On the **متطوع** form scroll to المرفقات: 8 upload tiles, then 4 link boxes. |
| L15 | خطوبتي — identity and personal data | **FIXED** | `e634261`. **13 of 13**: `marriage_form_screen.dart:793-983`; auto code `M-`+date+hex (`marriage.go:279`); marital status has **8 options including a separated-never-married one** (`:1541-1558`). Wording note: it reads "منفصل / لم يتزوج سابقاً", not your "باكر" | خطوبتي → ملفي → تسجيل: the الحالة الزوجية dropdown has 8 choices. Tell us if you want "منفصل / لم يتزوج سابقاً" renamed to "باكر". |
| L16 | خطوبتي — housing incl. previous-residence years | **FIXED** | `e634261`. **12 of 12**: `:986-1169`, incl. **سنوات السكن الحالي `:1083`، السكن السابق `:1090`، سنوات السكن السابق `:1096`** and the rent reveal `:1126`. Same الجانب=أخرى quirk as L5 | On the خطوبتي form pick نينوى and confirm the cascade, then نوع السكن = **إيجار** and confirm a rent box appears. Check the three "previous residence" boxes. |
| L17 | خطوبتي — education/health incl. الديانة, القومية | **FIXED** | `e634261`. **22 of 22**, including the two you specifically flagged: **الديانة `:1570`** and **القومية `:1337`**, plus لون البشرة `:1240`, الطول `:1607`, الوزن `:1600` | On the خطوبتي form confirm **الديانة** and **القومية** are their own fields, plus لون البشرة، الطول، الوزن. |
| L18 | خطوبتي — possessions, needs, attachments | **FIXED** | `e634261`. **14 of 14**: five yes/no ownership dropdowns (`:1352`–`:1416`), other assets, partner requirements, and 4 attachments + 3 social links. **Caveat: the CV/certificate uploaders are image-only** (`shared/utils/image_pick.dart` uses `image_picker`) — a PDF CV cannot be uploaded | On the خطوبتي form check the five ownership questions, then try to attach a **PDF** CV — it will only let you pick a photo. Tell us if PDF matters. |
| L19 | خطوبتي — per-field privacy picker | **STILL BROKEN (0 of 7)** | The marriage form has **no per-field privacy picker** — only a single whole-profile audience selector with 3 fixed values (`marriage_form_screen.dart:1614-1630`), which cannot express "show my age, hide my photo". The per-field screen that does exist (`field_privacy_screen.dart:277-299`) writes `user_profiles.field_privacy` and **governs nothing on `marriage_profiles`** | خطوبتي → ملفي → تعديل: look for a list letting you tick الاسم / العمر / الشهادة / الوظيفة / المحافظة / الصورة. There isn't one — only "who can see my profile" with 3 options. |
| L20 | Every field's required/optional flag admin-controllable; new fields with no code | **STILL BROKEN (half)** | **Works:** an admin can flip any *existing* field Required ⇄ Optional ⇄ Hidden with no code change (`FieldRulesPage.tsx:160-162` → `main.go:971` → `field_rules.go:114`, migration `056:22-24`), and the app consumes it. **Does not work:** there is **no create endpoint** — only `list`/`SetState`/`SetSearchable` exist, and `FieldRulesPage.tsx:29-99` carries four **hardcoded** label maps. A genuinely new field still needs a migration + a hand-written widget | Dashboard → **قواعد الحقول**, switch any field to **مخفي**, then open that form in the app — it disappears. Now look for an "Add new field" button on that page: there is none. |

## M. Donations, payments & sponsorship scheduling

| ID | What the client reported | Verdict | Evidence | How YOU can check it |
|---|---|---|---|---|
| M1 | Transaction Code with independent ranges per section | **FIXED (PRJ) — ACH has nothing to issue for** | A project donation now draws its reference from the `projects` namespace and comes back `PRJ-000001`. **The obvious fix would have broken every project donation** and is worth recording: `donations.donation_kind` carries a CHECK constraint listing exactly `('general','campaign','sponsorship','in_kind','operational')` (`001_full_v2.sql:390`), so writing `'projects'` into it fails the INSERT. The code NAMESPACE is therefore resolved separately from the stored kind (`donations.go` `codeSection`) — the row still reads general, which is true, while the reference says which project it funded. Campaign and operational donations keep their existing codes; precedence is pinned by tests. The arrival SMS is paged from the same section, and `sectionLabelAr` now falls through to the shared nine-section table — without that a `PRJ` donation would have texted the English word "projects" into an Arabic SMS. **ACH is not a gap:** "Our Achievements" is a read-only portfolio screen (`our_work_screen.dart`) with no payment surface at all, so a seeded namespace with no call site is correct there, not missing | Donate to a named project in the app: the reference should now start `PRJ-`. Dashboard → التبرعات shows it in the reference column. Achievements has nothing to pay for, so no `ACH-` code will ever appear unless you add a paid surface there. |
| M2 | Payment notification on every completed donation | **ALREADY OK** | Both legs fire. In-app: `notifyDonorOfSubmission` runs unconditionally after insert (`handlers/donations.go:230-231`). Section phone: `notifySectionArrival` (`donations.go:239,254-271`) sends section + amount + code. Phone and on/off are admin-editable per section (migration 027, `main.go:670`). **Two caveats, both configuration:** the SMS is silently skipped unless `OTPIQ_API_KEY` is set, and the donor's in-app notification carries amount + campaign but not the code or section | Dashboard → Donation Codes: set a phone on one section and make a test donation. That phone should get an SMS naming the section, amount and code, and your app bell should show a confirmation. |
| M3 | Five donation types offered at selection | **STILL BROKEN** | There is no five-way type step anywhere. Checkout stacks four unrelated selectors (`continue_donation_screen.dart:506,521,539,547`), and the widget actually named `_DonationTypeSelector` (`:1115`) offers general/zakat/sadaqah — not your five. Cash, electronic and balance transfer exist only as flat **payment-method rows**; "support the organization" is a destination tile (`8de1afd`); **in-kind is absent from the donation flow entirely** — its form lives in a different module a donor never reaches from the Contribute tab. **Latent bug:** `normalizeType` coerces anything outside cash/bank/wallet to `"bank"` (`paymentmethods.go:50-56`) | App → Contribute → pick an amount → Continue: you should see one screen asking what kind of donation this is with five choices. Instead there are four separate dropdowns, and no way to offer goods at all. |
| M4 | Electronic transfer branches into general vs project | **STILL BROKEN** | Both paths exist but **nothing branches on the payment method** — the destination choice renders *above* the payment list and nothing reads the chosen method. General aid has no explicit affordance; it is what you get by *not* picking a project. And the project dropdown has **no null / "no specific project" item** (`continue_donation_screen.dart:527-534`), so once a donor picks a project there is no way back. (The hide flags are correctly built.) | App → Contribute → Continue → choose a project, then try to change your mind and give to general aid instead: there is no "no specific project" option to go back to. |
| M5 | 22 named projects + add a new one with no code | **ALREADY OK** | **All 22** are present — exactly 22 INSERT rows at migration `085:21-44`, each matched to your list. Full admin CRUD at `main.go:949-953` with auto-slugging. **Caveat worth knowing:** the same table also holds 10 generic starter categories from migration `029:24-35`, all active — so the donor's dropdown shows **32 entries, not 22** | Dashboard → فئات المشاريع: count the rows — all 22 you asked for, plus 10 older generic ones you may want to switch off. The "Add" button creates a new project with no developer. |
| M6 | "امنح الآن" / "منح شامل" general fund | **ALREADY OK** | `_GiveNowCard` (`donations_section.dart:367`, rendered `:212`) labelled "تبرّع الآن / العطاء الشامل"; tapping it clears any campaign selection so the gift lands in the general fund. `622e6b0` **consolidated** rather than added (it deleted a 118-line duplicate). Caveat: the dashboard switch `donation_comprehensive_enabled` is seeded, served and parsed but **no widget reads it** — turning it off has no effect | App → Contribute tab: the large card at the top reading "تبرّع الآن / العطاء الشامل". Tap it and the amounts below become a general-fund donation with no project attached. |
| M7 | Donations extensible from the dashboard | **STILL BROKEN (1 of 4)** | Three of four are fully covered: payment methods (`main.go:1018-1022`), projects (`:949-953`), in-kind categories (`:960-964`), all with add/edit/reorder/delete and admin pages. **New donation *types* are not extensible** — there is no `donation_kinds` table and no admin route; the set is hardcoded in Go (`donations.go:293-301`). Also `/admin/donation-codes` is GET+PUT only, so a new *section* namespace still needs a migration | Dashboard → طرق الدفع and → فئات المشاريع both have working "Add" buttons. Look for an equivalent page to add a new **donation type** — there isn't one. |
| M8 | Sponsorship calendar with recurring due dates | **ALREADY OK** | Recurring dates are materialised as real rows. `advance()` supports weekly/monthly/quarterly/yearly (`sponsorshipschedule.go:39-49`); `Generate()` creates up to 12 future occurrences (`:59-105`), idempotent via a unique constraint; runs automatically when an admin activates a sponsorship (`admin_status.go:695`). Statuses recompute against today on every read; settling one date rolls `next_due_date` forward | Dashboard → الكفالات → open any monthly sponsorship: a list of dated future instalments is generated for you, and marking one paid moves the "next due" date forward on its own. |
| M9 | Reminders to donor and beneficiary (in-app, SMS, voice) | **STILL BROKEN (voice)** | Donor **and** beneficiary are both reminded, in-app **and** by SMS, on an automatic 6-hourly sweep (`sponsorship_schedule.go:96-136`), each stamped so it never double-sends; lead time is admin-configurable. **The voice leg is not wired to reminders** — `AppVoice` exists but its only caller is a manual read-aloud button on one screen | Dashboard → الكفالات → set a due date 1–2 days out, then check both phones and both in-app bells — both get a reminder. Nothing is read aloud; that is the missing piece. |
| M10 | Entitlement tracking screen | **ALREADY OK** | All four groupings are implemented as filter chips (`sponsorship_schedule_screen.dart:28-33`) — القادمة / المستحقة الآن / المتأخرة / السجل, each with its own empty state and status tag, backed by `GET /api/sponsorships/schedule` scoped to the caller as either side, with "overdue" genuinely recomputed against the current date. Minor gap: staff-skipped dates never appear, since History asks only for `paid` | App → Home → the Sponsorships tile → Overview → "جدول الكفالات": four tabs across the top. |

## N. Open questions — need owner clarification

| ID | What the client reported | Verdict | Evidence | How YOU can check it |
|---|---|---|---|---|
| N1 | Where is the volunteer ↔ beneficiary ↔ donor chat? | **NEEDS OWNER** | A donor↔beneficiary thread exists but is party-approved, not employee-approved, and leaks real names and phones (see K19). A general platform reaching visitors does not exist (K20). Which of the three surfaces is expected **in this release** is a scope decision | Open Messages in the app to see what exists today, then tell us which of the three chats you need first. |
| N2 | Where is the volunteer ↔ technical-team chat? | **NEEDS OWNER** | `casevolchat` and `staffchat` exist server-side; the staff space is dashboard-only and nothing in the app references it. Not built as a volunteer-facing surface | Scope question — tell us whether volunteers should reach the technical team through الدعم الفني or a separate chat. |
| N3 | Where is the خطوبتي registrant ↔ technical-team chat? | **NEEDS OWNER** | `marriagechat` exists with identity masking, but as an applicant↔applicant channel, not applicant↔staff | Scope question — confirm whether this is a new surface or the existing الدعم الفني. |
| N4 | Where is search in the marriage section? | **NEEDS OWNER** | It **exists** — `marriage_search_screen.dart:18-22,105` with staff-configurable filters, plus save-a-person. So this may already be answered by K14 | Open the marriage tab → البحث and confirm it is what you meant; if so, this row is closed. |
| N5 | Tapping a notification makes it vanish immediately | **NEEDS OWNER — mechanism proven** | **Reproduced live today, and it is by design.** Opening a notification calls `markAsRead` (`notifications_controller.dart:314`), and `filteredNotifications` (`:43-50`) excludes read items when the **"غير مقروء"** filter is selected — so it drops out of the list at once. With "الكل" selected it stays put and only loses its dot | App → الإشعارات → tap "غير مقروء", then open any notification: it disappears. Now tap "الكل" and try again: it stays. Tell us which behaviour you want. |
| N6 | Hide sponsorship details from beneficiaries; route to the volunteer | **NEEDS OWNER** | Scope and destination are undefined: "routed to the volunteer" could mean visible to the assigned volunteer only, or actioned by them. Not implementable without that answer | Tell us which sponsorship details should be hidden from the beneficiary, and what "routed to the volunteer" means. |
| N7 | Terminology conflict between the two PDFs | **NEEDS OWNER** | See I1 and I3 — both still blocked on your answer, and `TERMINOLOGY.md` is now partly stale (its الكفالات row was overtaken by `98629a7`) | Answer the two questions in I1 and I3 and every other rename in group I unblocks. |
| N8 | "نظام لوحة تحكم للمستخدم في العقد" | **NEEDS OWNER** | The phrase is ambiguous and nothing in the codebase corresponds to a shop-owner dashboard. K18 gives owners a submit-and-await-approval form only | Tell us what dashboard a shop or factory owner is meant to receive. |
| N9 | External integration with دائرة الرعاية | **NEEDS OWNER** | Same as F10 — needs written legal authorization and a technical channel before it can be estimated, let alone built | Confirm whether a signed data agreement and an API exist. |
| N10 | Some dashboard sections could not be reviewed | **NEEDS OWNER** | Confirmed as a real gap: this checklist describes only what you could reach. Expect a second wave once those sections hold data | Tell us which sections were empty and we will seed them with test data **before** the next review round. |
| N11 | Explain the duplicated "send request" | **NEEDS OWNER — answered** | Here is the explanation you asked for: there is **one** live entry point (the beneficiary Home hero button). The second lives on a screen that is **orphaned** — reachable only through the assistant chat — and the third is that chat's own route. The quick-actions duplicates were already deleted (`dashboard.dart:532-537`) | Sign in as a مستفيد: Home has exactly one إرسال الطلب and the quick-actions row has none. Confirm the orphaned screen can be deleted. |

---

## NEW FINDINGS — not on the client's list

Found while verifying. **Nothing was fixed;** these are reported only.

| # | Finding | Severity | Evidence |
|---|---|---|---|
| 1 | ~~**The user detail endpoint sends the bcrypt `password_hash` to the browser.**~~ **CONFIRMED AND FIXED (2026-08-15).** Reproduced at every tier — the hash was printed in the test output for `employee` as readily as for `super_admin`, because the route carries **no per-module permission at all** (main.go leaves the generic detail endpoint "ungated beyond RequireAdmin"), so `sensitive_data` never applied. **10 of 46 production rows carry a hash, two of them `super_admin`.** `google_sub` was leaking beside it. The redaction list is now inverted into a per-resource column allow-list (`detailColumns`), so a future migration that adds a secret is safe by default. **The LIST endpoint was already clean** — it selects explicit columns and exposes only a `has_password` boolean; a test now pins that. | **High** | Fix: `backend/internal/handlers/admin_detail.go`; test: `admin_detail_columns_test.go` |
| 2 | ~~**Any rank with "edit users" can change the Super-Admin's phone number.**~~ **CONFIRMED AND FIXED (2026-08-15).** Reproduced as a working takeover against the real request chain — an `admin`, an `employee` and a `supervisor` each rewrote a higher-ranked account's phone and the row changed. Now guarded by a shared tier floor + a Super-Admin-only rule on staff phone changes; see H13 above. | **High** | Fix: `backend/internal/handlers/admin_user_guard.go`; test: `admin_user_edit_guard_test.go` |
| 3 | ~~**15 of 31 admin delete routes bypass the trash entirely.**~~ **CONFIRMED AND FIXED (2026-08-15).** Route count verified independently: 31 admin DELETEs, 15 permanent. **13 converted**; `app_events` and `banned_words` kept permanent on purpose, each with a stated reason (see H15). | **High** | Fix: `backend/internal/handlers/admin_delete.go` (`trashRow`) + 13 handlers; test: `admin_delete_trash_test.go` |
| 4 | **Editing a payment method silently rewrites its type.** `normalizeType` coerces anything outside cash/bank/wallet to `"bank"`, so the Visa / MasterCard / mobile-transfer rows are corrupted the moment someone edits them in the dashboard — changing their type and their icon. | **Medium** | `backend/internal/paymentmethods/paymentmethods.go:50-56` |
| 5 | **All 9 city-guide entries have `sectors: []`.** Verified against the live API. The six sector filter chips therefore match nothing — picking any of them yields an empty list. This is a data problem, not a code one, and it may be what C2/A4 were really about. | **Medium** | `GET https://backend-production-59d2.up.railway.app/api/community` — every item returns `"sectors": []` |
| 6 | **A narrow logout race.** `logout()` awaits an HTTP POST with a 5-second timeout **before** clearing local storage (`auth_session.dart:163-177`). If the app is killed inside that window, the session is never cleared and the user is still signed in on next launch. I hit this once, during a simulator shutdown, and it produced exactly the A9 symptom. Clearing locally first, then revoking, would close it. | **Medium** | `humanitarian/lib/api/auth_session.dart:159-184`; `confirmLogout` at `widgets/settings_section.dart:160-161` |
| 7 | ~~**The notification detail dialog shows an English "Urgent" badge in the Arabic UI**, while the list card behind it correctly shows "عاجل".~~ **CONFIRMED AND FIXED (2026-08-15).** `d498453`. `notification_detail_dialog.dart:74` now renders `n.categoryLabel.tr` — the same expression the list tile at `notification_tile.dart:292` already used. Worth noting how this one hid: the Arabic entry **already existed**, so a map-coverage test would have passed while the defect was on screen. The test that catches it pumps the dialog and reads the badge. | **Low** | Fix: `humanitarian/lib/modules/notifications/widgets/notification_detail_dialog.dart`; test: `test/localization/localized_tag_test.dart` |

---

## Method

- **Code:** read at `main` (HEAD `ffd4ed8` at time of writing). Every "FIXED" cites the
  commit; every "STILL BROKEN" cites a file and line that reproduces it today.
- **Backend:** live probes against `backend-production-59d2.up.railway.app` (public
  endpoints only; admin endpoints correctly returned 401 and were not pursued).
- **Database:** production, **read-only SELECT queries only**. No data was modified and
  no connection string was written to disk.
- **App:** live on iOS Simulator `iPhone 17 Pro`, signed in as a donor, in Arabic.
  Role-gated beneficiary and volunteer screens were not reachable.
- **Dashboard:** the browser session had expired and credentials were not entered, so
  dashboard rows are code-grounded and say so where a live check was impossible.
- **Two corrections made during this pass, both caught by re-testing:**
- **(1) The deployment headline was wrong.** This report initially led with "A16 is live
  in production" as its top finding, based on the deployment log (last deploy 16:21, A16
  committed 16:49). A redeploy landed at 17:32 while the report was being written. Probing
  the live API directly showed the A16 fix **is** deployed, and the section was rewritten.
  The lesson is in the report: probe the running system, do not infer from timestamps.
- **(2)** A9 was initially recorded as reproduced —
  the app had returned to the iOS home screen after logout and relaunched still signed
  in. Re-testing showed the simulator had been shutting down at that moment. On a clean
  run logout navigated correctly to the sign-in screen and the session did **not**
  survive a force-quit, so A9 is recorded as FIXED, with the race it exposed logged as
  NEW FINDING 6.


