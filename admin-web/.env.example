# Copy to .env.local if you need to override the proxied default ('').
# When VITE_API_BASE_URL is empty, the SPA hits /api/* which Vite proxies to
# http://localhost:8080. In production builds, set this to your API origin.
VITE_API_BASE_URL=
